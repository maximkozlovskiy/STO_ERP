# MemoryManual — STO ERP

> Тонкий вхідний файл. Читається loop-ом щогодини і на початку кожної сесії.
> Містить ТІЛЬКИ поточний стан + останній commit + посилання на довідники.
> Вся історія → `CHANGELOG.md`. Патерни → `docs/PATTERNS.md`. Правила → `docs/BUSINESS-RULES.md`.

---

## Поточний стан

```
Дата:       2026-09-06
Фаза:       Активна розробка (CHANGELOG.md → docs/PHASES.md)
TypeScript: api ✅ 0 errors | web ✅ 0 errors (perf-аудит без правок коду) | shared ✅ 0 errors
Тести:      API ✅ 1255/1255 | Web ✅ 514+17 UX (row-status 10, useSavedFilters +3 view round-trip/back-compat, modal +4 Ctrl/Cmd+Enter) | E2E report-builder 9/9 + фінансові 74/74 + detail-panel-toggle 3/3 + cross-cache-invalidation 1/1 ✅
Останній:   2026-09-06 fix(tester) 53e45efd — Bug #643 HIGH: deep-link ?openReturn=<id> не відкривав модалку повернення. SR-модалка на purchase-orders — ОДИН інстанс з open={srCreateOpen}+editId={srEditId}; deep-link ефект виставляв лише srEditId → open лишався false → модалка мертва (toSupplierReturn з панелей нікуди не вів). Fix: resolvePurchaseOrdersDeepLink() у linked-nav.ts з явним srModalShouldOpen; ефект виставляє обидва setter-и. +service-тести SD/SR update() не нулить purchaseOrderId (edit preservation), SD count item1 (не-TRANSFER=1, soft-deleted-target=1). +resolver-тести (revert→2 fail). API 84/84, web linked-nav 8/8. tsc api/web ✅.
Tester:     2026-09-06 bug hunt Phase D «пов'язані документи» (StockDoc+SupplierReturn PO-пікер/FK, коміти 53c0e7b0..8db8a589). **1 баг HIGH (Bug #643) знайдено+виправлено, 1 розрив проаналізовано+прийнято (guard↔tx race, Item 3)**. #643 вище. Верифіковано без багів (нові дискримінуючі тести де були прогалини): count==detail в обидва боки (не-TRANSFER=1, TRANSFER=2, soft-deleted-target/PO=0); PO-picker create round-trip (валідний→персист+DTO, чужа-org/soft-deleted→BadRequest, без→null); edit не нулить purchaseOrderId; linked-counts валідація (empty/501/non-UUID→400, dup keyed once, cross-org→0); PO-picker supplier-scope (PurchaseOrderResponseDto.supplierId повертається). Item 3: PO-guard поза $transaction → soft-delete між guard/insert лишає FK на мертвий PO, але getLinked*/count фільтрують deletedAt:null → stale прихований в UI — прийнятно (не фіксимо). Playwright MCP timeout → service/contract/unit + reasoning. Нові підходи у SKILL.md (deep-link editId-без-open).
Попередній: 2026-09-06 fix(review) 8db8a589 — liveness-gating у getLinkedCounts (StockDocument/SupplierReturn). Count безумовно рахував PO/склад(и)/постачальника за наявністю FK, а detail фільтрує deletedAt:null → soft-deleted PO/склад/постачальник давав badge «1» + порожню панель (Bug #A/#641 count!=detail). Fix: findMany живих id + Set-membership перед інкрементом (дзеркалить invoices.getLinkedCounts). +2 spec (soft-deleted→count=0). API 82/82, web 40/40, tsc api/web ✅ 0.
Попередній: 2026-09-06 PO-пікер «Замовлення (джерело)» у create StockDocument/SupplierReturn (Phase D2). Опц. FK purchaseOrderId (міграція 20260905130000): DTO +@IsOptional @IsUUID, service персистить ?? null + опц. guard (purchaseOrder.findFirst orgId+deletedAt:null → BadRequest 'Замовлення не знайдено'), toDto +purchaseOrderId/purchaseOrderNumber (join number). Frontend: EntityPickerField + SearchPickerModal /purchase-orders (SR скоуп до постачальника client-side), create-only (edit read-only). Dirty: SR purchaseOrderId у deps; StockDoc id у form.
Попередній: 2026-09-05 вкладка «Документи» контрагента (Phase C) — commit aa152fb7. Backend GET /counterparties/:id/linked-documents (4 секції invoices/PO/supplier-payments/supplier-returns, orgId+deletedAt:null, take:500, Decimal→Number) + getLinkedCounts (groupBy deletedAt:null — count==detail, урок Bug #641). Route перед @Get(':id'). Frontend counterpartyLinkedConfig + вкладка через LinkedDocumentsPanel + useLinkedNav (toInvoice/toPurchaseOrder/toSupplierPayment/toSupplierReturn).
Review:     2026-09-05 code review Phase C (commit aa152fb7) — CLEAN, 0 виправлень. Перевірено: orgId+deletedAt:null на всіх 4 findMany і 4 groupBy (count==detail, Bug #641 тримається); FK-поля vs schema (Invoice=counterpartyId, PO/SP/SR=supplierId); getLinkedCounts zero-init кожного id + {} на empty + guard result[id]; route-ordering + ролі == findOne; hooks (useLinkedNav+useMemo) до early-return (hook-order); nav-таргети; mapRow null-safety; укр. лейбли. tsc api/web ✅ 0, vitest counterparties 61/61 (service 42, contract 19).
Optimize:   2026-09-05 perf-аудит UX-фіч (frontend only, diff 5433f932~1..HEAD). Висновок: 0 виправлень — код уже добре оптимізований (module-level Sets *_INACTIVE_STATUSES, hoisted nowMs через useMemo, стабільні useCallback deps, onSubmit у ref у base Modal, useUiFeatures module-cache, clearTimeout на create-path baseline). Знайдено 2 marginal observations (НЕ фіксовано, свідомо): (1) useUiFeatures() тепер у base Modal → фан-аут 3 idle-window-listeners × кожен інстанс + подвійна підписка у 5 великих модалках (спрацьовує рідко — login/logout/settings → marginal); (2) new Date(nowMs) per-row у rowStatusTone (page-limit ≤50 → µs, не варто чіпати pure-хелпер). Обидва патерни записано у sto-optimize «Накопичені підходи» (commit afaaa436). TypeScript web ✅ 0, тести modal 35 + useSubmitGuard ✅.
Tester:     2026-09-05 bug hunt «пов'язані документи» (Phases A+B, commits cb9a8e6c..b5feebeb). **1 баг знайдено+виправлено (Bug #641 MEDIUM), 1 ліміт задокументовано (#642 LOW)**. #641: getLinkedCounts (invoices/PO/supplier-payments) рахував зв'язок за наявністю FK, а getLinkedDocuments фільтрує deletedAt:null → count/detail розходились коли контрагент/банк-рахунок/каса/PO soft-deleted (досяжно: контрагента можна видалити під PAID-рахунком/RECEIVED-PO — delete-guard блокує лише відкриті док-ти). Fix: live-перевірка через findMany({id:{in},deletedAt:null}) + Set, батч без N+1. #642 (не фіксимо): mount-once []-deps deep-link ефект не спрацьовує на same-page ?open= self-nav — латентний, наразі недосяжний жодним config-шляхом. Перевірено (багів нема, guard-тести додано): duplicate/cross-org/zero-count ids, badge optional-chaining, панель missing/extra section key, nonexistent-UUID→error-банер, SP account без bank/cash + soft-deleted bank. Playwright MCP timeout → service/component/unit + reasoning. API 206/206 (+18), web 550/550 (+14, новий linked-nav.test), tsc api/web/shared 0. Нові тести дискримінуючі (revert→fail доведено).
Review:     2026-09-05 code-review фічі «пов'язані документи» (Phases A+B, diff 632bd3a9..HEAD). Висновок: 0 виправлень — код чистий. Backend (invoices/purchase-orders/supplier-payments getLinkedDocuments/getLinkedCounts): orgId+deletedAt:null скрізь (Payment append-only без deletedAt — коректно); getLinkedCounts zero-init кожного id + {} на пустий вхід + guard `result[id]` у groupBy-merge (жоден id не губиться); Decimal→Number на всіх money; take:500 cap; not-found→порожні секції без throw (дзеркало WO); LinkedCountsDto ArrayMinSize(1)/ArrayMaxSize(500)/IsUUID('all',{each}); linked-* routes ПЕРЕД :id; @Roles OWNER/ADMIN/ACCOUNTANT. Frontend: LinkedDocumentsPanel configRef/onLoadRef → fetch-deps [entityId,refreshKey,retryKey] (без churn); mapRow null-safe (displayCounterpartyName all-null→«(без імені)», fmt(Number), дати null-guard); deep-link ?open=/?openReturn= mount-once []-deps + UUID_RE + router.replace зберігає інші query-params (delete лише open); SP [id]/PageClient всі хуки перед early-return (hook-order стабільний); badge-cell skip zero + popup role=dialog+aria-label+Escape. API tsc 0, Web tsc 0, backend contract 79/79, web 41/41 (LinkedDocumentsPanel 10). Коміт не потрібен.
Tester:     2026-09-05 bug hunt UX-фіч (HEAD 15d54839) — **4 баги знайдено+виправлено (Playwright MCP timeout → fallback unit/component)**. #637 HIGH: Ф8 clone у списку гейтився лише useState cloningId → 2 POST /clone на native-click×2 (fix: новий хук useSubmitGuard sync inFlightRef). #638 MEDIUM: Ф6 Ctrl+Enter nested-modal scope-check непокритий → +2 тести. #639 HIGH: Ф5 unsaved-guard false-positive — async авто-вибір єдиного складу/філії осідав після setTimeout(0)-baseline → чиста форма хибно брудна (Escape→діалог); fix у 4 модалках (SR/PO/Stock/WO) через autoXRef-skip. #640 LOW: Ф1 rename «фільтр»→«подання» непослідовний (SaveFilterButton placeholder) + стейл-тест (9 baseline-падінь). Усі нові тести дискримінуючі (revert→fail доведено). Web-suite 55 файлів/540 тестів ✅ (було 531/9-fail), tsc web 0. Нові файли: hooks/useSubmitGuard.ts + 3 test-файли.
Review:     2026-09-05 фінальний code-review UX-фіч + perf-рефактор modal.tsx (diff 5433f932~1..HEAD, HEAD 3ad34270). Commit 49b9150d — 1 SUGGESTION-fix: Ф8 handleClone гейтив toast.success прапорцем features.toastEnabled, а сусідній toast.error у catch — ні (внутрішньоhandler-на неконсистентність §8.5); гейтнуто обидві гілки. Рефактор modal.tsx (ModalSubmitKeys монтується лише коли onSubmit заданий, useUiFeatures прибрано з base Modal) перевірено на 5 регресій — усі CLEAN: Escape працює для всіх модалок (handleKey у base, безумовний bind при open); panelRef populated до keydown (effect run post-commit, no first-keystroke race); Ctrl+Enter fires once + scoped (nested tests 2/2); cleanup на close/unmount (effect deps [open], unmount→removeEventListener); ModalProps API незмінний. useSubmitGuard sync inFlightRef коректний; autoXRef dirty-skip коректний (skip рівно раз, manual re-select до auto-value далі markDirty ✓). row-status.ts pure + nowMs hydration-safe у 3 сторінках. useTableColumns hydration-safe (localStorage у useEffect, JSON try/catch). tsc web 0, modal 35/35, useSubmitGuard 3/3, CreateWorkOrder 11/11.
Sync:       2026-09-03 Report Builder constructor 5-fixes audit — 1 fix (Dir3: ReportRunResult.columns type missing enumName?:string in backend interface; expandColumns already returned it). Commit 089b790e.
Tester:     2026-09-05 Фінал 4-го аудиту (mobile upload + web-модалки) — верифікація b2681f28+ca778c0b+0129d9c2, HEAD 0d213561 — **знайдено 5 нових багів (+1 meta), усі виправлено; готово до push**. calcVatTotals==backend recalcTotals ПО КОПІЙЦІ числово підтверджено (3×2.525@20 → VAT 1.52 aggregate обидві сторони; review-fix 0129d9c2 коректний). mobile getAccessToken повертає токен (не Bearer null). **АЛЕ grep double-submit по ВСІХ модалках виявив split-coverage:** round4 покрив 5 великих документних модалок, edit-модалки довідників + WO-add лишились без sync savingRef → same-tick дубль. Bugs #632 Counterparty / #633 Employee(+auth) / #634 Good / #635 WO-AddLine / #636 WO-AddPart (усі HIGH — silent duplicate master-data/labor/reservation). Fix: savingRef+setSavingBoth+`if(savingRef.current)return` першим рядком. **Meta (test-integrity): shipped SR double-submit тест (ca778c0b) був ХИБНО-ЗЕЛЕНИМ** — `userEvent.click`×2 дає React re-renderнути disabled={loading} між кліками → проходив і без guard. Переписано на native `HTMLElement.click()`×2 синхронно (гейт = sync-ref); дискримінацію доведено для всіх нових тестів (revert→«expected 2 to be 1»). +3 дискримінуючі тести (Good/Employee/AddLine)+SR-fix. Знахідка A (передіснуюча, LOW, НЕ round4): backend recalcTotals totalLabor=`round(Σ round)` vs totalAmount=`round(Σ raw)` розходяться на копійку при per-line дрейфі — кандидат окремого бек-фіксу. Web 503→514, tsc web/shared 0, API 1255/1255. **Playwright/Sentry MCP CONNECT_TIMEOUT → fallback unit/component + числова симуляція.** Попереднє: 2026-09-05 Фінал 3-го аудиту (isSystem-захист) — тест-верифікація комітів 635c8816 (backend+міграція) + 9af68c61 (frontend gating) + c836cbd4 (docs), HEAD c836cbd4 — **0 нових багів, готово до push**. Повний API suite ✅ 1255/1255 (87 файлів), Web ✅ 503/503 (46 файлів), tsc api/web/shared 0. Targeted specs РЕАЛЬНІ: payment-methods.service.spec (5: remove системного→BadRequest+updateMany.not.called; remove звичайного→updateMany; missing→NotFound; update name→BadRequest+update.not.called; update isActive системного→дозволено), currencies.service.spec (7, +isSystem-guard describe: remove/update системної UAH→BadRequest+updateMany.not.called; NBU-поля дозволені). **DB-verify (Prisma):** UAH isSystem=true; усі 5 методів (cash/card_terminal/bank_transfer/privat24_qr/monobank_qr) isSystem=true — backfill спрацював. **LIVE (nest build + kill PID 39528 + node dist/main.js, admin@sto.local):** (1a) DELETE UAH→400 «Системну валюту не можна видалити»; (1b) DELETE cash→400 «Системний метод оплати не можна видалити». (2) CREATE USD (isSystem=false)→201, PATCH name→200, DELETE→204; non-system payment method TESTPM→201/rename 200/delete 204. (3a) PATCH UAH лише nbuFetchEnabled→200 (NBU-поля редаговані для системної); (3b) PATCH UAH з name→400 «не можна перейменовувати або змінювати код». (4a) PATCH cash лише isActive→200; (4b) PATCH cash з name→400 «не можна перейменовувати». (6) GET currencies/payment-methods повертають isSystem у кожному item. Cleanup чистий: лише UAH + 5 системних методів активні, тест-записи (USD/TESTPM) soft-deleted. Frontend gating (CurrenciesTab/PaymentsTab): badge «Системна/Системний», delete прихований, name/code disabled, PATCH омітить name/code для системного → уникає false-400. Static: жоден інший модуль не мутує currency/paymentMethodConfig в обхід guard-ів (grep 6/6 — усі у guarded service). API log без несподіваних помилок (лише benign url.parse DeprecationWarning деп). Prev round: тест-верифікація комітів 3a4094e8 (audit-round3, 8 фіксів) + d7d1cee8 (docs) — 0 нових багів. Повний API suite ✅ 1246/1246 (86 файлів), tsc api/web/shared 0. Усі 3 нові spec + calendar spec РЕАЛЬНІ (не фіктивно-зелені): setup.service.spec (4: happy count=0/0→create+advisory-lock ПЕРШИМ у tx; race count=0-зовні/1-всередині→400+organisation.create.not.toHaveBeenCalled+advisory-lock узятий×1; outer count>0→$transaction.not.toHaveBeenCalled; email lowercase), zones.service.spec (зона з lift→BadRequest+zone.updateMany.not.toHaveBeenCalled; порожня→updateMany; count=0→NotFound), employees.remove-guard.spec (active WO line→BadRequest+$transaction+employee.updateMany.not.toHaveBeenCalled; без нарядів→updateMany+authAccount каскад; count=0→NotFound), calendar.service.spec findSlots CAL-C2 (ADMIN+MECHANIC: where.startAt має 'lt', where.endAt має 'gt' — half-open OVERLAP, НЕ containment). **LIVE (ПЕРЕЗБІР `nest build` + kill PID + `node dist/main.js`, admin@sto.local):** (1) warehouses.remove — WH-A з підтвердженим RECEIPT qty=5 (залишок>0)→DELETE 400 «на складі є ненульові залишки або резерв»; порожній WH-B→204 + зник з активного списку (регресія OK). (2) zones.removeZone — зона з lift TWO_POST→400 «у зоні є активні підйомники»; порожня зона→204 (регресія OK). (2c/2d) employees.remove — механік на DRAFT-наряді (active line)→400 «призначений на активні наряди»; ПІСЛЯ CANCELLED WO той самий механік→204 (термінальний статус не блокує); свіжий невизначений механік→204. (3) calendar findSlots — слот що починається 09-09 21:00Z й закінчується 09-10 06:00Z (startAt ПОЗА day-window 09-10) видимий у вибірці 09-10 разом зі звичайним in-day слотом (containment silently-drop усунено). (4) booking.create — минула дата (2020)→400 «не може бути в минулому»; майбутня робоча (2026-09-16 Wed)→201 (регресія OK). (5) setup outer guard live→403 «Повторна ініціалізація заборонена» (додатковий controller-guard ПЕРЕД service; TOCTOU-фікс покриває concurrent-кейс через advisory-lock+in-tx re-check — unit race зелений). Самовдосконалення: усі 8 патернів (TOCTOU count-ПОЗА-tx, containment-vs-overlap у read-фільтрі дати, cascade-remove-guards для агрегатів із FK-дітьми, past-date reject у public booking) вже присутні концептуально; додано новий підхід «read-query date-window має overlap-, не containment-семантику» + «cascade-remove-guard для master-data з FK-дітьми (MD-H1 class)» до Накопичених підходів. Попереднє: 2026-09-04 Фінал 2-го аудиту — тест-верифікація коміту 4a901e83 (audit-round2, 7 фіксів) + 4469e2ce (docs), main HEAD 4469e2ce — **0 нових багів, готово до push**. Повний API suite ✅ 1225/1225 (83 файли), tsc api/shared 0. Усі 7 affected-spec зелені (70 тестів) і РЕАЛЬНІ (не фіктивно-зелені): stock-doc CAS (order[0]==='cas' ПЕРЕД movement + count=0→createMovement not.toHaveBeenCalled), doc-number YEARLY (NULL+seq123→newSeq=1 не 124; поточний рік→інкремент; MONTHLY симетрія), sync tombstone LWW (stale v5<v10→update not.toHaveBeenCalled+accepted:1; fresh v9>v3→soft-delete), loyalty (dup documentId→account.update+tx.create not.toHaveBeenCalled). **LIVE (rebuild+restart dist, admin@sto.local):** CONFIRM подвоєння НЕ відтворюється — (A) два concurrent CONFIRM RECEIPT qty=7 → [201 CONFIRMED, 400 «Статус документу змінився»], залишок +7.0 РІВНО (не ×2); (B) sequential retry (confirm→confirm) → [201, 400], +5.0 рівно; (C) нормальний одиничний CONFIRM → 201, +5.0 рівно (регресія OK). Нормальна нумерація OK live (ПРХ-2026-000047/48/49 послідовно, рік у номері). API log без несподіваних помилок. Самовдосконалення: патерн «FSM read stale pre-tx → CAS-гейт першим у tx» уже у Накопичених підходах (Хвиля 1) — новий патерн не з'явився, SKILL без змін.
Review:     2026-09-05 Аудит коміту 635c8816 «fix(audit-round3): isSystem-захист Currency + PaymentMethodConfig (міграція)» (main → fix 9af68c61) — **1 IMPORTANT знайдено/виправлено (заявлений UI-гейтинг був відсутній)**. **Бекенд повністю КОРЕКТНИЙ:** (міграція) `ADD COLUMN IF NOT EXISTS "isSystem" BOOLEAN NOT NULL DEFAULT false` — безпечно на наявних даних (DEFAULT заповнює наявні рядки, NOT NULL не падає); backfill codes ТОЧНО збігаються з seed (UAH + cash/card_terminal/bank_transfer/privat24_qr/monobank_qr); ідемпотентна (IF NOT EXISTS + UPDATE...WHERE детерміністичні); backfill НЕ фільтрує deletedAt → навіть soft-deleted seed-рядки на розгорнутій БД маркуються isSystem=true. Жодна попередня міграція не додавала isSystem на ці таблиці (лише units/good_categories). (guards) remove — окремий cheap read isSystem + BadRequest + compound `updateMany where isSystem:false` (defense-in-depth); update currency блокує `dto.code!==undefined||dto.name!==undefined`, payment блокує `dto.name!==undefined` (code не в Update-DTO за побудовою); NBU-поля (nbuFetchEnabled/nbuMarkupPercent) і isActive/sortOrder/requiresFiscal лишаються редаговані — **update системної валюти з лише {nbuFetchEnabled} проходить** (спец тест підтверджує). orgId scope у всіх where. (resurrection edge, питання 3) currencies/payment-methods.create воскрешають soft-deleted рядок через `update({...dto, deletedAt:null})` — dto НЕ містить isSystem (server-only), тож isSystem=true **ЗБЕРІГАЄТЬСЯ** (не губиться) — прийнятно. (sync, питання 4) Currency+PaymentMethodConfig **взагалі не в sync push/pull** (0 у sync.service PUSH_SAFE/PULL_TABLES) → isSystem з вводу неможливий; Response-DTO +isSystem адитивне, non-breaking. (тести) 12/12 РЕАЛЬНІ (guard→updateMany/update not.toHaveBeenCalled на заблокованому шляху, toHaveBeenCalledTimes(1) на дозволеному). SystemTemplates-блок seed (currency/payment templates для onboarding) коректно БЕЗ isSystem — це каталог шаблонів, adopted-рядки йдуть через звичайний create (isSystem=false, видаляються). **ЗНАХІДКА IMPORTANT:** коміт-меседж заявляв «+isSystem у обидва Response-DTO (UI гейтить delete/edit)» — але frontend interfaces (ndi/types.ts Currency, ndi|settings/shared.ts PaymentMethod) НЕ містили isSystem і UI нічого не гейтив: кнопка delete UAH/Готівки активна (backend 400 — захист є, але UX-провал), а PATCH незміненого name/code системного запису повертав 400 (guard перевіряє !==undefined, не факт зміни). Fix (9af68c61, 6 файлів): +isSystem?:boolean у 3 interface (парність DTO); CurrenciesTab+PaymentsTab (ndi, live) — badge «Системна/Системний», delete прихований для системних, name(+code) disabled у модалці, save НЕ надсилає заблоковані поля для системних (лише NBU / requiresFiscal); toggle isActive методу лишається (сервер дозволяє); settings/PaymentsTab (dead-file, не імпортований) — дзеркалено body-omit проти sibling-drift. Дзеркалить UnitsTab isSystem-гейтинг. Cross-cutting чисто: 0 any/console.log/прямий-fetch/inline-HSL/[var(/BOM у 6 файлах; text-[10px] badge — наявна конвенція (22 usages). Розбіжність currency.update блокує rename (units.update — ні): навмисно суворіше (code=ідентичність для НБУ/обліку), безпечно. tsc web/api/shared 0; API currencies+payment specs 12/12. Попереднє: 2026-09-04 Аудит коміту 4a901e83 «fix(audit-round2): 7 логічних багів у раніше не аудитованих модулях» (main) — **0 багів (усі 7 фіксів верифіковані КОРЕКТНИМИ, без фіксу/коміту)**. Перевірено коректність кожного проти коду+схеми, не лише стиль. **(1) stock-documents CONFIRM CAS [CRITICAL]** — вікно РЕАЛЬНО закрите: CAS `updateMany where status:DRAFT` — ПЕРША операція у tx, ПЕРЕД усіма createMovement; count=0→throw→rollback усіх рухів; фінальний stockDocument.update прибрано але статус НЕ лишається невиставленим (CAS сам робить DRAFT→CONFIRMED); DOC_TRANSITIONS CONFIRMED досяжний ЛИШЕ з DRAFT (23) → hardcoded `status:'DRAFT'` у CAS не блокує легітимний перехід; happy-path цілий. **(2) document-number YEARLY reset [HIGH]** — **НЕ скидає СЕРЕД року**: UPDATE пише `lastResetYear=currentYear` на КОЖНОМУ next() (і reset, і non-reset шлях, рядок 80) → після першого виклику у 2026 `lastResetYear=2026`, наступний виклик того ж року `2026!==2026`=false→інкремент, не reset; межа року 2027 `2026!==2027`=true→reset до 1; NULL→reset до 1 (свіжий конфіг); нескінченного reset НЕМАЄ. MONTHLY не зачеплено (окрема умова, рядок 70-72). SELECT FOR UPDATE атомарність збережена. **(3) webhooks idempotency [HIGH]** — deliveryId=randomUUID() генерується ОДИН раз у dispatchEvent ПЕРЕД .add() (webhooks.service:160), у job payload → BullMQ retry перечитує той самий payload→той самий id (НЕ регениться); процесор читає deliveryId з job.data→X-STO-Delivery-Id+Idempotency-Key+body.id; HMAC signature=createHmac(secret).update(body) де body МІСТИТЬ id (processor:64-71)→підпис покриває body.id; fallback `deliveryId ?? String(job.id ?? ...)` коректний для legacy jobs. **(4) sync DELETE-tombstone LWW [HIGH]** — `BigInt(rec.syncVersion) > existingVersion` (strict `>`, sync.service:271) ІДЕНТИЧНО update-шляху (287); BigInt-порівняння коректне (schema syncVersion=BigInt, coerce number/string, `?? 0` fallback); не блокує легітимний свіжий DELETE (клієнт що інкрементує шле вищу версію); accepted:1 навіть коли tombstone НЕ застосовано (LWW no-op, не conflict — навмисно, тест підтверджує). **(5) completion-acts sign CAS [MEDIUM]** — CAS `updateMany where status:DRAFT` у tx; count=0→throw→rollback; WO→INVOICED update ПІСЛЯ CAS у тій же tx (лузер rollback-иться до нього); auto-invoice ПОЗА tx навмисно, лише winner (workOrderId присвоюється рядок 210 лише після успішного CAS) + guard `вже існує активний рахунок`. **(6) loyalty earn idempotency [MEDIUM]** — check `findFirst {accountId,EARN,documentId}` у ТІЙ САМІЙ tx що increment+create (151-179); `if(documentId)` гейт→null-documentId earns завжди нараховуються (manual accrual не блокується). Нюанс (НЕ регресія, codebase-wide READ-COMMITTED residual): два ІСТИННО-одночасні jobs з тим самим documentId можуть обидва пройти findFirst (немає @@unique[accountId,type,documentId]) — але домінантний threat (BullMQ sequential retry) повністю покрито (перший коммітить до retry); airtight-фікс=DB unique constraint, поза скоупом. **(7) warranties autoCreate addDaysKyiv [MEDIUM]** — семантика коректна: addDaysKyiv резолвить Kyiv-календарний день base через KYIV_YMD(sv-SE), +N днів через setUTCDate (DST-safe), повертає UTC-опівніч Date; expiresAt=DateTime (schema:2002, не @db.Date — UTC-midnight валідний); стара setDate використовувала server-local календар (01:30 Kyiv=23:30 UTC попереднього дня→зсув); warrantyDays з config (caller). Cross-cutting чисто: 0 console.log/any/BOM/російських слів у 8 файлах. API tsc 0; affected suites 85/85 (stock-documents 26, document-number 7, webhooks 18, sync 8, completion-acts 6, loyalty 13, warranties 7). Попереднє: 2026-09-04 Аудит коміту 99ea2ce9 «fix(audit-wave5): master-data guards + web MEDIUM (MD-H1/H2/M2, WEB-M9/M13)» (main, HEAD 99ea2ce9 → fix b6277a8f) — **1 MEDIUM знайдено/виправлено (WEB-M13 lost-update)**. Перевірено КОРЕКТНІСТЬ 5 пунктів; 4 бездоганні. **MD-H1** (counterparty remove guard): FK-поля правильні — WorkOrder.counterpartyId + PurchaseOrder.supplierId (звірено зі схемою 1052/1385). Статуси notIn коректні: WO `notIn [ARCHIVED,CANCELLED]` блокує DRAFT/ESTIMATE/APPROVED/IN_PROGRESS/ON_HOLD/COMPLETED/INVOICED/PAID — PAID/INVOICED свідомо блокують (осиротіли б на soft-deleted CP; борг покриває окремий balance-guard) — навмисно, не баг; PO `notIn [RECEIVED,CANCELLED]` лишає **PARTIAL** (частково отримане) як open — коректно. balance `Number()!==0` — Decimal 0.00→0 exact, zero-check безпечний. NotFound/BadRequest імпортовані. **MD-H2** (VIN unique): update `excludeId=id`→`NOT:{id}` виключає self; `if(!vin)return` пропускає null/undefined/empty; `deletedAt:null` — unique серед активних; licensePlate свідомо не гейтимо. **MD-M2** (cycle-guard): isSystem-guard (80) ПЕРЕД cycle-guard (84) — системна не тригерить getDescendantIds; visited-set (init parentId, check-before-push) розриває pre-existing DB-цикл; getDescendantIds не мутує вхід (локальні Map/array); self-parent `dto.parentId===id` окремо ВІД descendant-check (важливо: getDescendantIds НЕ включає сам id). **WEB-M9**: fmt()→fmtMoney лише для грошей (todayRevenue/revenueMonth/unpaidAmount); fmtInt лишився для nextMaintenanceMileage км (408) — без dead-import. **ЗНАХІДКА MEDIUM (WEB-M13, реальна регресія — флагована ризик-точка):** перехід із `setConfig(prev=>next)` на `setConfig(next)` з `config` у замиканні (deps [config,persist]) увів **lost-update**: два швидкі toggle в одному tick (double-click/Enter-repeat/синтетичні події ДО re-render) читали стейл-`config` → другий губив зміну першого. Обмін безпечного дубль-PUT (StrictMode) на втрату даних. Fix: `configRef.current` завжди тримає останній config; мутатори читають `next` із ref + синхронно оновлюють його → чейнінг послідовних викликів; persist() лишається ПОЗА updater (StrictMode-фікс збережено); reset() теж синхронізує ref. +1 регрес-тест (два toggle в одному act()→обидва поля; буга-версія давала лише ['email'] — верифіковано revert-ом). Cross-cutting чисто: 0 any/console.log/BOM у 5 файлах. tsc web/api 0, web 498→499, affected API 65/65 (counterparties 35/vehicles 16/good-categories 14). Попереднє: 2026-09-04 Аудит Хвилі 4 web-консистентності — коміти bdae5106 (4a cross-cache WEB-H1/H2) + 20614200 (4b idempotency+race WEB-H3/H4) + e4c8cb33 (4b залишкові cross-cache) (main, HEAD e4c8cb33 → fix c6ababa5) — **1 SUGGESTION (dead import) знайдено/виправлено, 0 логічних багів**. Перевірено КОРЕКТНІСТЬ усіх 6 пунктів. (1) **cache-invalidation.ts**: усі 9 імпортованих keys (workOrders/inventory/counterparties/stockDocs/supplierPayments/purchaseOrders/invoices/reports/dashboard) існують і мають `.all`; **0 циклічних імпортів** — lib→hooks одностороннє, жоден hook не імпортує cache-invalidation; НОВИЙ крос-хук useSupplierPayments→usePurchaseOrders БЕЗ зворотного (usePurchaseOrders→useInventory, не →useSupplierPayments) → циклу немає. Над-інвалідація dashboard.all/reports.all на КОЖЕН рух — **прийнятна**: invalidateQueries лише РЕФЕТЧИТЬ активні (змонтовані) queries, неактивні лише маркує stale → refetch на наступний mount; dashboard/reports не змонтовані під час роботи на work-orders/settlements → вартість мінімальна. (2) **WEB-H3 idempotency**: SupplierPaymentCreateModal — createdIdRef скидається у resetForm:227, resetForm викликається useEffect([open]) при !open:230-232 → retry після успіху+обриву НЕ дублює, АЛЕ close+reopen для НОВОГО документа скидає ref (через !open) → другий легітимний create НЕ блокується (підтверджено новим тестом). InvoiceCreateModal — createdInvoiceRef скидається на open:208; handleCreate reuse на retry, після кожного успішного POST /lines рядок видаляється зі state:400 → mid-batch retry не дублює; після повного успіху ref=null:403. **Edit DELETE+POST** (handleSave): initialLineIdsRef наповнюється на load:250, removedIds=snapshot∖current:436, після кожного DELETE `initialLineIdsRef.current.delete(lineId)`:439 → retry не re-DELETE (404-safe); рядки не пропускаються (recompute зі скороченого snapshot коректний). (3) **WEB-H4 race-guards**: SettlementsTabContent (reqId=++ref ПЕРЕД await:110, перевірка ПІСЛЯ:128/132/137), vehicles/PageClient (ok()=()=>reqId===ref.current читає ref СВІЖО), WorkOrderAddPartModal (uomReqRef bump ПЕРЕД await + clearGood bump-cancel), EmployeeEditModal (cancelled-flag з cleanup `return () => {cancelled=true}`, setCache безумовний бо кеш≠стан) — усі happy-path не зламані (перший вибір показується). (4) **Немає stale-closure** у ref-guardах — усі читають `ref.current` у момент resolve, не захоплюють значення. (5) **purchase-orders**: прибрані inventoryKeys/supplierPaymentsKeys/counterpartiesKeys справді більше не вживані (grep 0); purchaseOrdersKeys ЛИШИВСЯ (used:1302). (6) web tsc 0, 0 any/console.log/React.X/BOM, українська коректна (0 російських слів). **ЗНАХІДКА SUGGESTION:** stock-documents/page.tsx — після заміни `qc.invalidateQueries({queryKey: stockDocsKeys.all})` на `invalidateStockDocumentSideEffects(qc)` import `stockDocsKeys` став dead (єдина згадка = рядок import). tsc не ловить (noUnusedLocals off), ESLint no-unused-vars ловить. Fix: прибрано зі списку import. Web тести 495→496 (+1 WEB-H3), усі зелені. Попереднє: 2026-09-04 Аудит коміту de35bf31 «fix(audit-wave3): єдине округлення грошей (roundMoney) + Kyiv-дата ТО» (main, HEAD de35bf31 → fix f7a935db) — **0 логічних багів у коміті + 9 залишкових un-rounded грошових шляхів поза скоупом закрито**. Перевірено КОРЕКТНІСТЬ усіх 6 пунктів аудиту: (1) **roundMoney** — half-away-from-zero (1.005→1.01, 2.675→2.68), +1e-9 НЕ ламає великі суми (999999.99→unchanged, 999999.995→1000000.00, 12345678.905→12345678.91 — епсилон крихітний відносно ×100 масштабу, лише нудж exact-half), від'ємні симетричні (-1.005→-1.01), `Number.isFinite` guard (NaN/Infinity→0) — numeric sanity 13/13 PASS. (2) vat.ts calcLineVat/sumLineTotals/calcDocVat — усі гілки квантовані; **подвійне округлення НЕ дає розходження stored-rows↔stored-total** бо `amount`/`totalWithVat` сумують ТІ САМІ per-row `priceWithVat` значення що зберігаються, кожна колонка незалежно `roundMoney(Σ)`; totalWithoutVat+totalVat може відрізнятись від totalWithVat на ±0.01 — це inherent VAT-rounding (стандартно, не баг). (3) **WO-H1** in-tx re-read `db.workOrder.findFirst totalAmount` — `db=tx??prisma`, writeOffPartsAndCharge ЗАВЖДИ викликається з `tx` (transition:711) → tx-client; fallback `wo.totalAmount??0` недосяжний (WO вже re-read як `fresh` у тій же tx); `chargeAmount<=0`→throw ПЕРЕД createTransaction; atomicity ціла (усі read/write через `db`). (4) sumLineTotals — без розходження (див. п.2). (5) **addDaysKyiv** — бере Kyiv-календарну дату base (KYIV_YMD), +N днів через UTC-арифметику на 00:00Z (DST-safe, без hour-shift); nextMaintenanceDate — `DateTime?` (не @db.Date), 00:00Z-репрезентація date-only коректна; стара `setDate` на non-Kyiv сервері зсувала календарний день. (6) documentDate у invoices/WO/PO/SR/stock — усі `@db.Date` (schema:1075/1393/1451/1503/1536/1733) → Prisma повертає 00:00 UTC → `dateTo lte` UTC-порівняння коректне, dateTo лишено правильно. **ЗНАХІДКА (9 un-rounded поза скоупом хвилі, виправлено f7a935db):** completion-acts.buildLines `amount:quantity*unitPrice` + PDF reduce total (юридичний акт, audit L3 — float у документі й сумі); invoices 3× `priceWithVat=priceWithoutVat+vatAmount` без roundMoney (updateLine:522 + refresh manual VAT 696/714 — асиметрія з addLine:462, feeds sumLineTotals ПЕРЕД округленням); purchase-orders totalAmount/totalVat (create:249 + update:377/379), **receivedAmount:538 (живить SUPPLIER_CHARGE борг постачальнику!)**, display amount:905; supplier-returns totalAmount (create:149 + update:257) + display amount:517; loyalty.redeem discountAmount (points×дробовий redeemRate, повертається+у notes); xlsx import amount:646; reports stock-value value:228+totalValue:237. Усі через roundMoney (DB-write пути були DB-rounded on-store → функційно безпечні, але порушували заявлений інваріант «кожен грошовий результат через roundMoney»; receivedAmount — справжня severity бо fresh-обчислення живить баланс). Cross-cutting чисто: 0 console.log/any/BOM у 7 файлах. API tsc 0, тести 1183/1183 (+8 roundMoney). Попереднє: 2026-09-04 Аудит коміту dc026ca6 «fix(audit-wave2): ідемпотентність грошей + анти-race слотів (FIN-C1/C2, CAL-C1)» (main, HEAD dc026ca6 → test 48847c7a) — **0 логічних багів + 1 test-gap закрито (FIN-C1 без тестів)**. Перевірено КОРЕКТНІСТЬ кожного фіксу проти коду+схеми+DTO. **FIN-C1** (payments.service CAS оплати): CAS `updateMany where status:'SENT'→PAID` виконується ПЕРШИМ у tx, ПЕРЕД `payment.create`/settlement/checkbox-черги; два concurrent create → лише один count=1, другий count=0 → throw → rollback (жодних грошових side-effects). Вікно закрито коректно. **Standalone-payment (без invoiceId І без workOrderId)** — CAS у гілці `if (dto.invoiceId)`, тож для чистого авансу анкера немає → ідемпотентність відсутня, АЛЕ це inherent design (довільний платіж без документа-анкера; будь-яка ERP це дозволяє), НЕ регресія цього коміту й не fixable без client idempotency-key. Нюанс (не баг): `inv===null` (invoiceId на видалений/неіснуючий інвойс) мовчки пропускає весь блок — платіж усе одно створюється; лояльно, не double-spend. **FIN-C2** (invoices.transition CHARGE): `chargesStandaloneOnSend = status===DRAFT && newStatus===SENT && workOrderId===null` → CHARGE ЛИШЕ для standalone; WO-рахунок не отримує подвійний CHARGE (борг уже при COMPLETED). `amount=Number(inv.amount)` — коректне джерело (selected:297). CAS `updateMany where status:inv.status` (pre-read) гейтує ВСІ newStatus (CANCELLED/PAID теж), не ламає інші переходи. **CAL-C1** (EXCLUDE gist): predicate `deletedAt IS NULL AND liftId IS NOT NULL` коректний — слоти без lift не блокуються (NULL≠NULL), half-open `[)` = app-overlap `startAt<end AND endAt>start` (дотик не конфлікт). Split-day self-conflict НЕможливий: parent (→20:00) і child (next-day-start→) не перетинаються попри той самий liftId; updateSlot блокує слоти з continuation рано (368). ⚠️ Обсяг backstop: EXCLUDE лише по liftId — employee-double-booking лишається app-probe-only (racy), але це заявлений скоуп («одного підйомника»). **throwIfExclusionConflict**: матчить `23P01 || constraintName`, rethrow-ить не-exclusion as-is (app BadRequestException «Підйомник зайнятий» не містить 23P01/constraint → лишається 400); ConflictException імпортований; `:never` → tsc бачить catch як non-returning → `slots`/`updated` гарантовано assigned (tsc 0 підтверджує). **Тести FIN-C2 РЕАЛЬНІ** ($transaction mock виконує callback з prisma як tx → CAS/createTransaction реально спрацьовують): standalone→CHARGE(500), WO→not.toHaveBeenCalled, count=0→throw+no-CHARGE. **ЗНАХІДКА (test-gap):** флагманський CRITICAL FIN-C1 (CAS грошей) НЕ мав ЖОДНОГО прямого тесту — spec-файлу payments.service не існувало; коміт-меседж «payments CAS покрито логікою» вводив в оману. Додано payments.service.spec.ts (3 тести: count=0→throw+no side-effects; happy count=1→CAS ПЕРЕДУЄ create via invocationCallOrder + settlement PAYMENT; not-SENT→throw ПЕРЕД CAS). Cross-cutting чисто: 0 console.log/any/BOM у 4 файлах; усі findMany мають take. API tsc 0; payments+invoices+calendar 90/90 (87→90). Попереднє: 2026-09-04 Аудит коміту 88bfec4f «fix(audit-wave1): 3 CRITICAL логіки складу — подвійний резерв, коефіцієнт UoM, SKU restore» (main, HEAD 88bfec4f) — **0 багів (без фіксу/коміту)**. Перевірено КОРЕКТНІСТЬ кожного фіксу проти коду+схеми, не лише стиль. **WO-C1** (reserveParts лише при `wo.status==='APPROVED'`): коректно — FSM (work-orders.fsm.ts) дозволяє IN_PROGRESS ЛИШЕ з APPROVED (перший вхід) та ON_HOLD (повернення); ON_HOLD у RESERVATION_ACTIVE_STATUSES → резерв зберігається, повторне резервування подвоїло б reserved. `wo.status` — pre-tx статус, re-валідується in-tx (fresh.status!==wo.status→throw). CANCELLED-release НЕ зламаний (гейт RESERVATION_ACTIVE_STATUSES, окрема гілка, не залежить від WO-C1). reserveParts викликається ЛИШЕ з transition:707 — інші update-сайти (431 update, 1359 recalc, 1702 shareToken) статус=IN_PROGRESS не ставлять → жодного пропущеного входу. **WO-C2/C3** (lookup GoodUoM за {unitOfMeasureId:{in},goodId:{in}}, ключ мапи `goodId|unitOfMeasureId`): premise ПІДТВЕРДЖЕНО — addPart:1193 зберігає `WorkOrderPart.unitOfMeasureId = uomJunction.unitOfMeasureId` (FK на UnitOfMeasure.id), а НЕ GoodUoM.id (PK) → старий lookup по GoodUoM.id ніколи не збігався → coeff=1. Крос-збіг (goodA+uomB якщо обидва у списках) НЕможливий: findMany може повернути зайвий рядок реальної пари, але споживач читає мапу за ТОЧНИМ `part.goodId|part.unitOfMeasureId` → фантом лишається невикористаним. `@@unique([orgId,goodId,unitOfMeasureId])` (schema:967) гарантує ≤1 рядок на пару → ключ не колізує. Усі 3 споживачі (reserveParts:828, releasePartReservations:859, writeOffPartsAndCharge:890) використовують виправлений fetchPartCoefficients через `coeffMap[part.id]`; parts читаються повним findMany (goodId+unitOfMeasureId присутні); baseQty=quantity/coeff у всіх трьох. **MD-C1** (restore guard SKU+internalCode): guard перевіряє ОБИДВА поля (умовно на truthy), `NOT:{id}` виключає сам товар, `deletedAt:null` фільтрує лише активні дублі, happy-path не зламаний (клаш немає→updateMany). Нюанс (SUGGESTION-doc, не баг): коментар каже «немає DB-констрейнта» — але `@@unique([orgId,internalCode])` (schema:926, non-partial index goods_orgId_internalCode_key) РЕАЛЬНО існує → internalCode-клаш недосяжний (deleted-рядок тримає слот), guard лише конвертує потенційний P2002 500 у охайний ConflictException UA. SKU-guard (@@index only, non-unique) — справжній фікс. **Регресій-тести реальні** (не фіктивно-зелені): WO-C1 негативний асертить `partsFindMany not.toHaveBeenCalled()` (reserveParts — єдиний caller findMany на цьому шляху; проти старого коду впав би); WO-C1 позитивний — called; WO-C2 асертить `where.unitOfMeasureId.in` + writeoff `quantity===-2` (20/coeff10; старий код=-20 упав би); share-public WO-C3 асертить `where.unitOfMeasureId.in`+`where.goodId.in`; MD-C1 ×3 (ConflictException+updateMany-not-called / happy / NotFound). Cross-cutting чисто: 0 console.log/BOM/bare-any у обох сервісах. API tsc 0; affected suites 66/66 (goods 39, work-orders 18, share-public 9). Попереднє: 2026-09-04 Аудит коміту e4f2ed2e «експорт CSV/XLSX склеєних рядків у плоскому режимі» (main, HEAD e4f2ed2e → fix 1e3c7258) — **1 IMPORTANT знайдено/виправлено (екран↔експорт розбіжність)**. Перевірено 5 вимог: (1) header==body==totals по кількості колонок у ОБОХ станах showCount — усі три `1(№)+cols+(showCount?1)+agg`, showCount у експорті `detail.some(__mergedCount>1)` ІДЕНТИЧний екранному `flatHasMerge` (у flat-режимі `showCount=hasGroups||flatHasMerge` зводиться до flatHasMerge) → колонка «Склеєно» присутня/відсутня синхронно; CSV та XLSX з одного `rows` → узгоджені. (2) exportCell: enum→enumLabel (гілка `if(enumName)`), date→fmtDate(String), null/undefined→'', число як сирий рядок (для XLSX `ss:Type=Number` детект по `i>0 && c!=='' && !isNaN(Number(c))` — колонка 0 завжди String). (3) CSV escaping — кожна клітинка `"..."` з `"`→`""` (RFC 4180), BOM літерал підтверджено od=efbbbf (U+FEFF); XLSX esc &<> у text-content (лапки/апострофи не потребують escape поза атрибутами). (4/5) tsc web 0, українська, 0 any/console.log/React.X/BOM. **ЗНАХІДКА IMPORTANT (екран↔експорт):** exportCell НЕ мав boolean-гілки → boolean-колонка (`isActive`/«Активна», type:'boolean' у registry, user-selectable) рендерилась `String(true)`→"true"/"false" (англійською), тоді як екранний fmtCell дає «Так/Ні» → розбіжність файл↔екран + порушення україномовного UI (CLAUDE.md #15). Fix: додано `if(type==='boolean' && typeof value==='boolean') return value?'Так':'Ні'` ПЕРШОЮ (дзеркалить fmtCell); гейт typeof→null у boolean-колонці все одно дає ''. Числове форматування навмисно лишається сирим у експорті (grouping/decimals на екрані через fmtMoney/fmtInt, у файлі raw для number-детекту) — прийнятна різниця, не розбіжність лейблів. Агрегатні клітинки flat: екран '—', файл '' — косметична, не баг. Скоуп: 1 файл, +6 рядків (boolean-гілка + docstring). Попереднє: 2026-09-04 Аудит коміту 0fe3ee80 «Report Builder: склеювання ідентичних рядків (mergeDetailRows) + авто-згортання + fill height + «Сформувати»» (main, HEAD 0fe3ee80) — **0 багів (без фіксу/коміту)**. Перевірено КОРЕКТНІСТЬ (не лише стиль): (1) mergeDetailRows — числові колонки визначаються по type∈{number,decimal} (симетрично NUMERIC_COL_TYPES); signedByType (stockMovement.quantity/WRITEOFF/non-physical) застосовується ЧЕРЕЗ numericValue — той самий шлях що computeAggs SUM → знак НЕ дублюється; читання type-поля з СИРОГО row (не з проєкції) → merged sum == grand SUM навіть коли `type` поза columns. Немає мутації вхідних rows (будуються нові bucket.row). **КЛЮЧОВА ПЕРЕВІРКА merge-key**: спершу флагнув як ambiguous (join міг склеїти РІЗНІ рядки якщо scalar-значення містять роздільник) — АЛЕ роздільник виявився NUL-байтом `\0` (`join('\0')`, git diff рендерив його як пробіл; od -c підтвердив 0x00), а Postgres text НЕ дозволяє NUL → колізія неможлива за побудовою; помилкова тривога, фікс не потрібен. (2) Консистентність сум: Σ__mergedCount==rowCount (unit-тест reduce==rowCount; кожен сирий рядок інкрементує рівно один bucket.count); порожня вибірка→[], all-unique→кожен count=1 (тест), all-same→1 bucket. tfoot «Кількість»=rowCount (==Σ). (3) Perf: O(N·C), N≤5000 cap, C=мала жменя колонок; Map get/set O(1); 0 nested row-loops, 0 O(N²) — прийнятно. (4) showCount=hasGroups||flatHasMerge (flatHasMerge=some(__mergedCount>1)) — ховає колонку коли всі distinct, показує при дублях; вирівнювання ІДЕНТИЧНЕ у 4 місцях (thead/flat-body/tfoot/empty-colSpan усі 1+cols+(showCount?1)+agg); grouped-режим GroupRows завжди рендерить node.count td + leaf placeholder → 0 drift в обох станах; typeof __mergedCount==='number' коректно narrow-ить unknown (detailRows:Record<string,unknown>[]). (5) ResultView flex-1 min-h-0 fill-height — layout-ланцюг ПОВНИЙ: page-fill(flex-col,flex:1,min-h:0,overflow:hidden)→ReportBuilder root(flex flex-1 min-h-0 flex-col)→ResultView root(flex flex-1 min-h-0 flex-col)→scroll-div(flex-1 min-h-0 overflow-auto); sticky top-0 thead працює (scroll-контейнер=цей div); контрол-ряд+amber-банер отримали shrink-0; видалення max-h-[60vh] безпечне (висота від flex-контексту). (6) Авто-згортання повернено `if(sortOverride===undefined) setConfigCollapsed(true)` — re-sort НЕ згортає; палітра ДОСЯЖНА через компактну панель (441-471, aria-expanded=false → розгортає grid) → регресія «не групується» НЕ повернена (E2E асертить). Примітка (pre-existing, поза скоупом): export flat-режиму (exportReport:1216) емітить ЛИШЕ 1 summary-рядок «Усього», НЕ merged detail-рядки що видно на екрані — дискрепанс дані-екран/CSV, але успадкований (flat ніколи не експортував деталі), не регресія цього коміту. Cross-cutting чисто: 0 any/React.X/console.log/inline-HSL/rgba(var/[var(/pixel-arbitrary/BOM. API tsc 0, web tsc 0, aggregator 18/18, web 495/495. Попереднє: 2026-09-04 Аудит коміту e0a90dcf «прибрано авто-згортання Report Builder (ховало палітру К/Г/Ф → «не групується») + порожній простір» (main, HEAD e0a90dcf → fix 10a92645) — **1 SUGGESTION знайдено/виправлено (a11y)**. Корінь «третього повернення не групується» усунено правильно: `setConfigCollapsed(true)` після run прибрано → палітра з кнопками К/Г/Ф лишається, групування досяжне будь-коли (бекенд працює, live-verified 9 груп). Рендер-структура коректна й взаємовиключна: `{!entity ? placeholder : null}` + `{entity && configCollapsed && <button compact>}` + `{entity && <grid className={cn(..., configCollapsed && 'hidden')}>}` — компактна панель показується САМЕ коли grid схований (0 подвійного показу); ResultView винесено ОКРЕМО (рядок 586, поза grid) → згортання не ховає результат. JSX збалансований. page.tsx: `{tab !== 'builder' && (` (рядок 238) обгортає весь scrollable-content, закритий на 557 `)}`, placeholder «Оберіть параметри» тепер усередині → 0 порожнього простору під builder; інші таби (revenue/vat/settlements/work-orders/stock/load/profitability) рендеряться всередині, не зламані. configCollapsed НЕ dead: хедер-тоггл (386, aria-expanded), компактна панель (розгортає), grid (ховається). ЗНАХІДКА (SUGGESTION a11y): компактна панель-`<button>` — disclosure-контрол, але без `aria-expanded`; додано `aria-expanded={false}` (узгоджено з хедер-тогглом що має `aria-expanded={!configCollapsed}`; WAI-ARIA disclosure pattern; MemoryManual трекає configCollapsed aria-expanded як фокус-точку цього компонента). Компактна панель інакше a11y-ок: `type=button`, `focus-visible:outline-*`, `title`, українська, лише nested `<span>` (валідний HTML). +2 E2E у коміті (палітра лишається; згорнута панель показує вибір; палітра hidden при collapse). Cross-cutting чисто: 0 any/React.X/console.log/inline-HSL/rgba(var/[var(/BOM; text-[13px]/[11px] — pre-existing font-size arbitrary конвенція dense-UI (не layout-px). web tsc 0, тести 495/495. Попереднє: 2026-09-04 Аудит коміту 6405c3a9 «DetailPanelToggle як стандарт списків — Купівля + Склад» (HEAD 6405c3a9 → fix 3fd7d5fb, main) — **1 IMPORTANT + 1 SUGGESTION знайдено/виправлено**. IMPORTANT (purity/StrictMode): `selectPO` робив `apiFetch` довантаження повного PO ВСЕРЕДИНІ `setSelectedPO(prev => ...)` — side-effect у state-updater; під React 18/19 StrictMode dev double-invoke updater дублював GET `/purchase-orders/:id`. Fix: fetch винесено ПОЗА updater; toggle-рішення (клік по вже-вибраному рядку → закрити) через `selectedPOIdRef` (синх ref, sync-ується useEffect([selectedPO])) — без стейл-замикання; stale-guard `cur?.id === po.id` для гідрації збережено. SUGGESTION: російське «это» у коментарі рядка 287 → українською (правило uk-UA). Перевірено РЕАЛЬНУ підключеність selection-state (НЕ orphan як Bug #496): row onClick `if (detailPanel.enabled) selectPO(po)` → `setSelectedPO` → `<DetailPanel open={!!selectedPO && detailPanel.enabled}>`; selectedPO консюмиться title/subtitle/tabs + reset у delete-handler. row/panel гейтяться `detailPanel.enabled` в ОБОХ (PO + inventory); edit/delete/checkbox cells мають `onClick=stopPropagation`. buildPoTabs/panelConfigFields useMemo/useCallback deps=[panelConfig.config] стабільні (без re-mount). inventory: тогл лише goods-mode (`viewMode==='goods'`), панель гейтиться enabled → toggle-off ховає. §8.5 grep-детектор проходить (0 lists із DetailPanel без DetailPanelToggle; 10 файлів). Cross-cutting чисто: 0 React.X/any/console.log/BOM; PurchaseOrder/POLine типи (lines/linesCount/supplierName/paymentDate/goodName/unitShortName/amount) усі існують. web tsc 0, тести 495/495. Попереднє: 2026-09-04 Аудит коміту 0729e639 «7 логічних багів обліку — партії/баланс/собівартість» (HEAD 0729e639 → fix d74b7b3d, main) — **1 SUGGESTION знайдено/виправлено (float-drift)**. Перевірено коректність КОЖНОГО з 6 фіксів (не лише стиль): (1) BATCH_CREATING_INFLOW={RECEIPT,OPENING_BALANCE} симетрично у ОБОХ гейтах (needsCostLookup + createFromReceipt); TRANSFER-receipt приходить як RECEIPT з явним price (weightedCostPrice джерела) → createFromReceipt все одно спрацьовує, cost-lookup пропускається бо price задано — коректно; OPENING_BALANCE тепер створює партію → Σremaining==quantity тримається. (2) in-tx re-read статусу у work-orders.transition + purchase-orders.receive — ДО side-effects (reserveParts/writeOffPartsAndCharge), українською, happy-path не зламано; дзеркалить існуючий supplier-payments.confirm (той самий plain-SELECT + plain-update патерн — READ COMMITTED residual gap для істинно-одночасних TX є codebase-wide design, НЕ регресія цього коміту; ловить домінантний double-click/sequential-retry кейс). (3) over-reservation guard `reservedDelta>0 && reserved>quantity` — гейт reservedDelta>0 НЕ тригериться на RESERVATION_RELEASE (delta<0) ні на WRITEOFF/RECEIPT (delta=0); строгий `>` узгоджений з pre-check. (4) getAvgCost без LIMIT — SUM агрегує в БД (один рядок незалежно від N партій, 0 perf-regression), orgId у WHERE обох гілок. (5) pricing scope — ТОЧНО дзеркалить resolveRule precedence goodId>brandId>goodCategory>goodType>global (взаємовиключні виміри, найспецифічніший); field-names звірено зі схемою (category/goodType/brandId). (6) QTY_EPSILON=1e-9 — не ламає цілі кількості (5−5=0 exact), квантизує float-дрейф. ЗНАХІДКА: consumeBatch увів epsilon для isActive-квантизації, але exhaustion-check лишився `remaining > 0` → span по дробових партіях (0.3−0.1−0.1−0.1≈2.7e-17) кидав би хибне «бракує 2.7e-17 одиниць»; fix `remaining > QTY_EPSILON`. Cross-cutting чисто: 0 any/console.log/BOM, всі findMany мають take. API tsc 0, тести inventory+work-orders+purchase-orders 343/343. Попереднє: 2026-09-04 Report Builder «підказка групування + прибрано колонку Кількість=1» (HEAD 915ab374 → fix 2319d550, feat/supplier-payments) — **1 SUGGESTION знайдено/виправлено (a11y)**. Банер «Групування не задано» з'являється динамічно після запуску звіту → не оголошувався скрінрідером; додано `role="status"` (implicit aria-live=polite, узгоджено з search-combobox/spinner/entity-picker-field). `aria-hidden` на ℹ️ вже коректний (декоративне емодзі). Ключова перевірка colspan/вирівнювання: `showCount=hasGroups` gated ІДЕНТИЧНО у 4 місцях — thead th, detail body row (немає count td, бо showCount=false у flat), tfoot td, empty-state `colSpan={totalCols}` (totalCols перераховано з `(showCount?1:0)`). Grouped-режим: GroupRows завжди рендерить count td (node.count) + leaf placeholder `<td/>` → 0 drift у ОБОХ режимах. Flat: thead=1+cols+0+agg == body=1+cols+agg == tfoot=1+cols+0+agg == colSpan. Dark mode банера повний парний набір (bg-amber-50/text-amber-900 + dark:bg-amber-950/40/text-amber-200, патерн TabBar.tsx:33). Cross-cutting чисто: 0 React.X/any/console.log/inline-HSL/pixel-arbitrary; українська коректна. TS web ✅ 0. Скоуп: 1 файл, +3 рядки (role+обгортка). Попереднє: 2026-09-04 Report Builder клік-кнопки К/Г/Ф (HEAD bbaa84e2 → fix 7b3b3cfe, feat/supplier-payments) — **1 IMPORTANT знайдено/виправлено (a11y)**. Огляд коміту «клік-кнопки К/Г/Ф на полях — групування без drag»: ZoneBtn є нативним `<button type="button">` (aria-label/title/disabled коректні); `addToZone` спільна для click і drop (єдине джерело правди — 0 sibling-drift, підтверджує cycle 3 pattern); guards `groupBy.length >= 5`, `!f.groupable`, `!f.filterable` з toast.warning; active/disabled стани візуалізовані; drag лишається резервним. Проблема: `<button>` не мав `focus-visible:outline-*` → клавіатурний фокус невидимий (WCAG 2.4.7 «Focus Visible» fail). Fix: `focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-ring` (дзеркалить patтern у components/ui/button.tsx:21). TS: web ✅ 0. Скоуп: 1 файл, +1 рядок. E2E-тест на клік-групування (е2е spec +16 рядків, `getByRole('button', {name:/групування/i}).click()`) уже включений у коміт bbaa84e2. Sibling-drift check: `text-white` on `bg-primary` — canonical STO pattern (button.tsx:28, command-palette.tsx:307), не помилка. `size-5`=20px — прийнятне для inline letter-chip у щільній палітрі (WCAG 2.5.8 recommend 24px, але 20px — акцептовано для dense builder UI). Попереднє: 2026-09-03 Report Builder 5-fixes audit (HEAD a9112413) — 1 CRITICAL знайдено/виправлено. `normalizeKey()` у report-aggregator групувала date-поля через `d.toISOString().slice(0,10)` → UTC-день, не Kyiv-день. Для DateTime полів (stockMovement.createdAt/payment.createdAt/settlementTransaction.createdAt) операція о 01:30 Kyiv (=22:30 UTC попереднього дня) потрапляла у попередній UTC-бакет → зсув фінансово-звітних даних на межі доби. `@db.Date` поля (documentDate тощо) не постраждали (Prisma повертає 00:00 UTC). Fix: KYIV_YMD = Intl.DateTimeFormat('sv-SE', {timeZone:'Europe/Kyiv'}) — той самий підхід що у kyiv-date.ts. +2 регресії (літо EEST UTC+3, зима EET UTC+2, межі доби Kyiv). Інші 5 фокус-точок (enumLabel fallback, configCollapsed aria-expanded, displayGroupValue node.field, ResultView layout, палітра ENUM_LABELS) — чисто. Попереднє: 2026-09-03 Report Builder pivot-fixes cycle 2 — 6 fixes. Nothing critical.
Tester:     2026-09-04 Хвиля 5 (ФІНАЛЬНА) наскрізного аудиту LIVE — 99ea2ce9+b6277a8f (main, HEAD 8dd9bcdc→docs b1419f34) — **1 MEDIUM (Bug #631) знайдено/виправлено + WEB-M9 test-gap закрито + усі 5 фокус-пунктів LIVE PASS**. Playwright/Sentry MCP CONNECT_TIMEOUT → §5.4 прямі API (admin@sto.local, Python urllib) + unit/component. **КРИТИЧНА ОПЕРАЦІЙНА ЗНАХІДКА (stale dist рецидив, урок Хвиль 2-3):** запущений `dist/main` (PID старт 20:27) СТАРШИЙ за rebuild wave-5 (dist 21:30) → MD-H2 VIN-guard мовчки не спрацьовував (dup VIN→201) попри зелений unit+свіжий dist-файл+grep. `node dist/main` не hot-reload. Fix: rebuild+kill+restart → всі guard-и запрацювали. **MD-H2 LIVE 7/7:** create VIN=A→201, dup→400, VIN=B→201, update→dup→400, update→own→200 (self-exclude NOT:{id}), порожній→skip, 2×null не колізує. **MD-M2 LIVE 5/5:** self-parent→400, перенос у нащадка C/B→400 «цикл» (не hang), валідний reparent C→A→200, rename→200. **MD-H1 LIVE:** чистий CP→204, активний DRAFT-наряд→400, DRAFT-PO→400, після CANCELLED→204 (happy). **АЛЕ виявлено Bug #631.** **WEB-M13:** useDetailPanelConfig 12/12 (тест «два toggle в одному act»→обидва поля; буга-версія=['email']). **WEB-M9:** format.test.ts +4 guard (fmtMoney padding копійок vs fmtInt варіативний). **Bug #631 MEDIUM (виправлено):** MD-H1 remove-guard блокував наряди/PO/баланс, але пропускав відкриті рахунки. SENT/OVERDUE ловились balance-guard-ом (CHARGE), АЛЕ DRAFT-рахунок ще без транзакції→balance=0→крізь усі guard→активний інвойс осиротів на soft-deleted CP (live-відтворено: DELETE CP з DRAFT invoice→204, GET invoice→200 з мертвим parentId). Fix: +invoice.count({notIn:[PAID,CANCELLED]}) симетрично WO/PO→400 «має відкриті рахунки». +3 spec (mock+invoice.count). Live-verified: DRAFT invoice→400, після CANCELLED→204. API 1197→1200, web 499→503, tsc 0/0/0. SKILL.md +§1.1 checklist (remove-guard relations-completeness, balance≠proxy) + 2 approaches (remove-guard completeness/orphan; stale-dist рецидив). **Фінал наскрізного аудиту (5 хвиль): весь suite зелений, tsc чистий — готово до push.** Попереднє: 2026-09-04 Хвиля 4 (web-консистентність) LIVE-тестування — bdae5106+20614200+e4c8cb33+c6ababa5 (main, HEAD 498bfc90) — **1 HIGH (Bug #630, родина з 5 модалок) знайдено/виправлено + LIVE cross-cache PASS**. Playwright MCP CONNECT_TIMEOUT → §5.4 fallback Playwright CLI. **(1) Component:** SupplierPayment idempotency + модалки 5/5. **(2) LIVE cross-cache (WEB-H1/H2):** новий E2E cross-cache-invalidation.spec — RECEIPT CONFIRM через UI («Підтвердити документ» → invalidateStockDocumentSideEffects) → SPA-навігація на «Залишки» → кількість +7 exact (PASS). Статично: усі 9 invalidation-keys prefix-и mounted queries; callsite-и у всіх transition/confirm/receive. **(3) WEB-H3:** retry-after-error PASS, АЛЕ **concurrent double-submit FAIL → Bug #630**. **(4) WEB-H4 race-guards:** SettlementsTab(reqId)/vehicles(ok())/AddPart(uomReqRef)/Employee(cancelled) — усі коректні, 0 багів. **(5) Регресії:** invoices E2E 23/23, WO+inventory 15/15, PO-receive+stock-doc 9/9 PASS (1 pre-existing flake crud-purchase-order:241 row-click, НЕ пов'язаний зі змінами — падає й на baseline). **Bug #630 HIGH (виправлено):** create-модалки захищені ЛИШЕ `disabled={saving}` (спирається на re-render МІЖ кліками) → два click-и в ОДНОМУ tick (швидкий double-click/синтетичні події/Enter-repeat) обидва входять у handler до застосування disabled → 2 POST → 2 документи. idempotency-ref (createdIdRef) НЕ рятує (виставляється після await 1-го POST). Виявлено нативним подвійним dispatchEvent (fireEvent маскував: sync-flush між кліками→1). Fix: `if (savingRef.current [|| transitioningRef.current]) return` першим рядком у 5 модалках (SupplierPayment+Invoice+PurchaseOrder+StockDocument+WorkOrder — усі мали savingRef, але create-шлях не гейтили; SupplierPayment отримав новий savingRef). +2 component-guard (нативний dispatch, revert→2/fix→1). web tsc 0, vitest 496→498. SKILL.md +§1.3 checklist+grep (#630) + 1 approach «concurrent double-submit обходить idempotency-ref». Попереднє: 2026-09-04 Хвиля 3 (точність грошей: roundMoney) LIVE-аудит — de35bf31+f7a935db+3b037422 (main, HEAD 3b037422→fix eefa72ec) — **1 LOW-MEDIUM (Bug #629) + усі 6 фокус-пунктів PASS (2 LIVE)**. Dev-сервер up; dist містив wave-3 код (не stale). **(1) roundMoney** 38 edge-кейсів PASS (1.005→1.01, 2.675→2.68, великі 9999999999.995→1e10, від'ємні симетр., NaN/±Inf→0, +1e-9 не псує .xx4999). **(2) Σ==total** LIVE: invoice 100.10×3×20 → pwv 300.30/vat 60.06/pwvat 360.36, amount==totalWithVat, pwv+vat==priceWithVat; фракційні 33.33×3+0.125×8 → Σstored-rows==кожна total-колонка exact, всі 2-знакові. **(3) WO-H1 CHARGE** LIVE: IN_PROGRESS(144)→COMPLETED → CHARGE=144=in-tx re-read totalAmount, баланс +144. **(4) PO receivedAmount→SUPPLIER_CHARGE** LIVE: price 33.33, receive 3 → receivedAmount 99.99 (не 99.98999…), SUPPLIER_CHARGE=99.99, баланс −49683→−49782.99 (delta −99.99 exact). **(5) addDaysKyiv** 6 DST-кейсів PASS (зима/літо, межі доби, spring/autumn DST). **(6) Регресії** 255 money-тестів + усі VAT-режими квантовані. **Bug #629 LOW-MEDIUM (виправлено):** reports.service похідні гроші БЕЗ roundMoney — `totalCostLabor=totalLabor×0.4` (3520.30×0.4=1408.1200000000001), Σ-reduce (revenue/workOrders/settlements totals), різниця vat.net — дрейфують у IEEE-754, маскуються fmt() на екрані, але просочуються СИРИМИ у CSV-експорт «Рентабельність» (page.tsx:620,623) + JSON API. Fix: roundMoney на кожне похідне money-поле (profitability/revenue/workOrders/settlements/vat). Test-gap закрито: reports.service.spec.ts (5 тестів; раніше 0 unit на весь ReportsService). API tsc 0, тести 1183→1188. SKILL.md +§1.1 checklist (#629) + 1 approach «money×дріб/reduce/різниця сирим у export/JSON». Попереднє: 2026-09-04 Хвиля 2 наскрізного аудиту LIVE — FIN-C1/C2 + CAL-C1 (main, HEAD f27df90d→f967a4de, коміт dc026ca6+48847c7a) — **1 MEDIUM (Bug #628, robustness) + 3 CRITICAL wave-2 LIVE-верифіковані PASS + критична операційна знахідка (stale dist)**. Playwright/Sentry MCP CONNECT_TIMEOUT → верифікація прямими API (admin@sto.local) + Prisma-пробники. **CAL-C1 PASS:** DB — 2 пересічні слоти на (orgId,liftId) → 23P01 `calendar_slots_no_overlap` (INSERT І UPDATE); дотик `[12,13)` після `[10,12)` → ОК (half-open). API — пересічний POST → 400 app-probe; concurrent 2×POST → рівно 1 слот (201)+1×400, НЕ 500/НЕ два. Міграція 20260904120000 застосована, btree_gist встановлено. **FIN-C1 PASS:** standalone-рахунок SENT → 2 concurrent payments → рівно 1 CREATED+1×400 «уже оплачено (паралельна)» (CAS count=0→rollback); рівно 1 Payment, статус PAID; повторна оплата PAID → 400. **FIN-C2 PASS (після rebuild):** standalone SEND → баланс +CHARGE (транзакція CHARGE ... Invoice реально записана), оплата → net-zero; баланс тест-CP відновлено до вихідного. **Регресії PASS:** нормальне бронювання 201; invoice DRAFT→SENT→(оплата)→PAID happy 201/201. **КРИТИЧНА ЗНАХІДКА (process):** запущений `dist/main` був СТАРОЮ збіркою → FIN-C2 CHARGE мовчки не спрацьовував на live попри коректний код+юніти+`dist`-grep; rebuild+restart → PASS. Урок: unit-зелений ≠ deployed. **Bug #628 MEDIUM (виправлено, f967a4de):** `syncWorkOrderSlots` (alternate-mutation `by-work-order`) не обгортав `$transaction` у throwIfExclusionConflict як createSlot/updateSlot → concurrent race на UPDATE парента (EXCLUDE діє і на UPDATE) → 23P01 → generic 500 замість 409. Fix: try/catch→throwIfExclusionConflict + 2 regression-guards. API 1176→1178, tsc 0. SKILL.md +§1.1 checklist (#628) + 2 approaches (error-mapping-symmetry; stale-dist verification-hygiene). Попереднє: 2026-09-04 Хвиля 1 наскрізного аудиту LIVE — WO-C1/C2/C3 + MD-C1 (main, HEAD ba006f3b, коміт 88bfec4f) — **1 LOW (Bug #627, robustness) + 3 CRITICAL wave-1 LIVE-верифіковані PASS**. Playwright/Sentry MCP CONNECT_TIMEOUT → верифікація прямими API-викликами (Python urllib, токен admin@sto.local) + Prisma-пробники БД; API перезібрано з джерела і перезапущено. **WO-C1 (головний фікс):** наряд з qty=2 → APPROVED→IN_PROGRESS reserved=2 → ON_HOLD reserved=2 (зберігся) → IN_PROGRESS знову reserved=2 (**НЕ подвоївся до 4**), 2й цикл теж 2; COMPLETED qty 100→98 reserved→0. **WO-C2/C3:** товар з GoodUoM «Пак» coeff=10, позиція 2 «Пак» → IN_PROGRESS reserved=0.2 (=2/10, коефіцієнт застосовано, було б 2 при багу); COMPLETED qty→99.8 (writeoff 0.2, було б 98); findOne unitShortName=«Пак» (codepoints 0x41F/0x430/0x43A)+coefficient. **MD-C1:** SKU=X delete→новий X→restore першого = 409 Conflict UA; після видалення дубля restore=201. Direct-Prisma підтвердив guard знаходить дубль. **Регресії FSM:** happy-path APPROVED→IN_PROGRESS→COMPLETED ✓; release IN_PROGRESS→ON_HOLD→CANCELLED reserved→0 ✓. Уточнення дос'є (не баг): work-order.md каже «CANCELLED з IN_PROGRESS», але FSM дозволяє IN_PROGRESS→{ON_HOLD,COMPLETED} лише; CANCELLED-з-резервом досяжний з ON_HOLD (код=правда). **Bug #627 LOW (robustness, знайдено під час live-MD-C1):** bodyless POST (/restore,/transition) + Content-Type: application/json + порожнє тіло → Fastify FST_ERR_CTP_EMPTY_JSON_BODY (FastifyError, не HttpException/Prisma) → провалювалась у else HttpExceptionFilter → 500 «Внутрішня помилка» + Sentry-шум. Не регресія wave-1 (передіснуюча поведінка фреймворку); веб-клієнт уже захищений (api-client.ts:59-68 не шле CT без тіла). Fix: guard isFastifyClientError (code FST_ERR_CTP_* І statusCode 4xx) → чистий 4xx UA + logger.warn (без Sentry); 5xx-FastifyError і Node errno лишаються 500. LIVE-verified: empty-body restore 500→400, normal 201, WO-C1/C2/MD-C1 усе ще PASS. +4 регресії у http-exception.filter.spec.ts. SKILL.md +1 approach «Fastify content-type-parser помилка → 500 замість 4xx». Baseline: api/shared/web tsc 0, API 1166→1170/1170, affected 344/344. Попереднє: 2026-09-04 Report Builder mergeDetailRows — ЖИВА перевірка склеювання 0fe3ee80+4f643e91 (main, HEAD aba30e62) — **1 MEDIUM (Bug #626, UX) + LIVE eyes-on скрін склеювання**. Playwright MCP CONNECT_TIMEOUT → §5.4 fallback: Playwright CLI + скріншоти. **СКЛЕЮВАННЯ РЕАЛЬНО ВИДНО** (скрін): «Рядки закупівель»→колонки Товар+Кількість(К)→«Сформувати» → хедер «рядків: 334», у таблиці **7 рядків** (по унікальному товару), НЕ 334 однакових. Dup1-E2E-DUP-019061 склеєно зі 124 рядків, quantity просумовано=636. Інваріант Σmerge-count==rowCount(334). Усі 6 сценаріїв ✅: (1) склеювання видно; (2) кнопка «Сформувати» (не «Запустити»); (3) авто-згортання→компактна панель «Групування:— · Колонки: Товар, Кількість» + палітра досяжна; (4) fill height (таблиця до низу, БЕЗ порожнього банера); (5) Наряди→Сума(К)+Статус(Г)→9 груп drill-down (Затверджено 6·3520,…, Архів 2·600); (6) суми коректні. Merge-key=NUL(`join('\0')`, od підтвердив 0x00→колізія неможлива, тривога рев'ю знята правильно). **Bug #626 MEDIUM UX (виявлено скріном головного сценарію):** merge-count колонка мала хардкоджену назву «Кількість» → у сценарії Товар+Кількість дві сусідні колонки «Кількість» (сума quantity vs лічильник склеювання) — неоднозначність. Fix: label умовний `{hasGroups?'Кількість':'Склеєно'}` (grouped=розмір групи node.count лишається «Кількість»; плоский merge=«Склеєно»). Eyes-on after fix: «Товар\|Кількість\|Склеєно\|Σ Кількість». Тест-baseline тримається: web tsc 0, vitest 495/495, API 1160/1160, E2E report-builder 9/9. SKILL.md +1 approach «хардкоджений label службової колонки колізує з користувацьким полем даних». Попереднє: 2026-09-04 Report Builder «досі не групується» (3-тє повернення) e0a90dcf+10a92645 (main, HEAD efd29858) — **1 MEDIUM (Bug #625, test-integrity) + LIVE-верифікація головного сценарію скрінами**. Playwright MCP недоступний (CONNECT_TIMEOUT) → §5.4 fallback: Playwright CLI + скріншоти, інспекція очима. **Групування РЕАЛЬНО ВИДНО** (скрін): Наряди→К «Сума»+Г «Статус»→Запустити → таблиця Група\|Сума\|Кількість\|Σ Сума з 9 групами (Затверджено 6·3520, …, Оплачено 60), Разом 85, drill-down (▾ aria-expanded → детальні рядки). Українські enum-лейбли. Корінь усунено правильно: після Запустити палітра К/Г/Ф лишається (скрін). Усі 6 сценаріїв ✅: (2) палітра лишається; (3) під конструктором нема банера «Оберіть параметри…» (лише власний CTA), інші таби не зламані; (4) «Згорнути»→компактна панель «Групування: Статус→Пріоритет · Колонки: Сума»; (5) 2-рівнева ієрархія (18 aria-expanded); (6) дата-групування Kyiv (15 груп YYYY-MM-DD, Σлистків==grand 99453). Бекенд live: groupBy[status]→9 груп/85/30478 (==UI). **Bug #625 MEDIUM (test-integrity):** E2E test 3/test 4 асертили `page.locator('button[aria-expanded]').first()` — матчив «Згорнути»-тоггл хедера (ReportBuilder.tsx:386), НЕ рядок-групу → проходили б і при плоскому/порожньому результаті (той самий клас «не групується × 3»). Нюанс: groups-only (без columns) → backend не шле node.rows → групи disabled без aria-expanded (коректно). Fix: +колонка (rows→expandable) + scope до `table` + `thead first=='Група'` + count>1 + no-банер. Тест-фікс, не код-баг (продакшн групування коректний). SKILL.md +1 approach «weak page-scope aria-expanded асерт матчить decoy-тоггл». Baseline: web tsc 0, vitest 495/495, report-builder E2E 8/8. Попереднє: 2026-09-04 DetailPanelToggle-стандарт списків 6405c3a9+3fd7d5fb (main, HEAD 2a026a61) — **1 MEDIUM знайдено/виправлено (Bug #624) + E2E gap закрито**. Live-тест (Playwright CLI) Купівля/Склад: головний фікс (реальне selection-підключення PO, НЕ orphan Bug #496) LIVE-верифіковано скріншотом — клік рядка→панель з №·постачальником·вкладкою «Позиції» (5шт×100=500₴)·«Відкрити замовлення». pencil→edit-modal (stopPropagation) ✓. Склад: тогл goods-only, панель гейтиться enabled ✓. Bug #624 MEDIUM: після disable→enable тогла клік по last-selected рядку PO → мовчазний no-op (selectedPO/selectedPOIdRef не скидались при вимиканні → re-enable+клік того ж рядка = toggle-close). Fix: useEffect скидає selectedPO+ref коли !enabled (дзеркалить SKILL Bug #310-#311). inventory не уражений (клік=set без toggle-close). E2E gap: 11 списків з DetailPanelToggle без жодного панель-E2E → додано e2e/detail-panel-toggle.spec.ts (3 тести, зелені; off→on-цикл і виявив Bug #624). SKILL.md +1 approach «toggle-close selection без reset при disable + Playwright toBeVisible false-positive на w-0 overflow-hidden». Baseline: web tsc 0, vitest 495/495. Попереднє: 2026-09-04 Аудит обліку 0729e639 + d74b7b3d (main, HEAD 39e22085) — **1 CRITICAL + 2 MEDIUM знайдено/виправлено (Bug #621/#622/#623)**. Усі 7 фіксів коміту 0729e639 підтверджено коректними: (1) OPENING_BALANCE→StockBatch — LIVE-verified end-to-end: OB(100) CONFIRMED→StockBatch remainingQty=100 isActive=true (Σremaining==quantity), WRITEOFF(40)→quantity 60; (2)(3) concurrent-guard WO.transition + PO.receive — in-tx re-read, happy-path не зламано (спец-тести оновлені: existing тести повертають same-status→proceeds, +1 changed-status→throw); (4) over-reservation post-check `reservedDelta>0 && reserved>quantity` — не тригериться на RELEASE/WRITEOFF; (5) getAvgCost без LIMIT — batch/pricing тести зелені, AVG_COST weighted ок; (6) QTY_EPSILON exhaustion + isActive — дробове списання ок; (7) Bug #178 pricing scope precedence — логічно звірено з resolveRule. **Bug #621 CRITICAL (виявлено ПІД ЧАС live-верифікації item #1)**: `stockItem.upsert` create-гілка вставляла від'ємний quantity → Prisma upsert=`INSERT VALUES(quantity) ON CONFLICT DO UPDATE`, Postgres перевіряє CHECK `stock_items_quantity_nonneg` (міграція 20260902210000) на INSERT-tuple ДО арбітражу конфлікту → `23514` 500 на КОЖНОМУ WRITEOFF/TRANSFER-out/WO-COMPLETED, НАВІТЬ коли рядок існує зі 100 і DO UPDATE дав би 60. З моменту мерджу міграції на main кожне фізичне списання падало (unit-мок Prisma не б'є Postgres → CI зелений; сам баг створений backstop-фіксом Bug #613). Fix: `create.quantity = Math.max(0, quantityDelta)` (дзеркалить reserved-кламп). Live-verified after fix: RECEIPT→WRITEOFF 201, OPENING_BALANCE→WRITEOFF 201. **Bug #622/#623 MEDIUM**: regression-guard gaps — Bug #178 pricing scope (AND→precedence) без тесту → +1 unit (brand+category скоупить по brandId); QTY_EPSILON (d74b7b3d) без дробового epsilon-тесту → +2 unit (3×0.1 для 0.3 не кидає; 0.5 vs 0.3 кидає). +4 regression guards, API 1155→1159, tsc 0. SKILL.md +1 CRITICAL checklist у §1.1 + 1 approach «Prisma upsert × Postgres CHECK». Попереднє: 2026-09-04 Report Builder click-buttons К/Г/Ф (HEAD bbaa84e2..7b3b3cfe, feat/supplier-payments) — **1 LOW UX знайдено/виправлено** (Bug #606). Guard-order в `addToZone`: при 5/5 groupBy повторний клік по вже-активному чіпі «Г» кидав misleading `toast.warning('Максимум 5 рівнів групування')` — гард `groupBy.length >= 5` стояв ВИЩЕ за `!groupBy.includes(key)`. Fix: `includes` перенесено ПЕРШИМ (`return;` no-op); дзеркально виправлено гілку filters. Регресія: +1 E2E `Bug #606: повторний клік «Г» на вже-активному полі при 5/5 — без toast «Максимум»` — додає 5 groupBy → повторно клік Г → `toast toHaveCount(0)` + `«Групування (5/5)»` стабільно. SKILL.md +1 grep-паттерн у §1.3 (`includes/has` перед `toast.warning` limit-guard) + 1 approach «Guard-order у inline add-to-collection helper». Guards §1.1-§1.7 інакше чисто: К має `active` без disabled (будь-яке поле — колонка), Г/Ф правильно `disabled={!groupable/filterable}`, drag-fallback ділить `addToZone` (нема sibling-drift), toast для non-groupable/non-filterable dead-code у click (button disabled), досяжно з drop. Cross-check date-групування по Kyiv-дню (успадковано з HEAD a9112413 tester audit) — стабільно. Baseline: TSC web 0/0, API 1151/1151, Web 495/495, E2E report-builder 5/5. Попереднє: 2026-09-04 Report Builder feedback 5-fixes (HEAD a9112413, feat/supplier-payments) — **0 нових багів**. Live-verified 16 сценаріїв через curl API: (1) Kyiv-day групування 3 DateTime-сутностей — усі YYYY-MM-DD, `∅` для null; (2) Cross-midnight репліка (20 payment + 5+ settlement рядків 21:00-23:22 UTC = наступний Kyiv-день) — server-side агрегати ІДЕНТИЧНІ до Python-обчислених `astimezone(+03:00).strftime('%Y-%m-%d')`, доказ що UTC-shift не трапиться (payment 2026-08-30 sum=1200, settlement 2026-09-02 sum=25770 — обидва містять cross-midnight записи); (3) інваріант Σ листкових==grandTotal — payment 26500==26500, settlement 99453==99453; (4) 8/8 date-полів groupable без exception; (5) 8/8 enum-полів carry enumName у response.columns (WorkOrderStatus, InvoiceStatus, SettlementTransactionType, StockMovementType, CounterpartyType×2, PurchaseOrderStatus, WorkOrderPriority); (6) enum-значення СИРІ (frontend перекладе); (7) 9/9 сутностей run ok; (8-9) 2-рівневе групування date↔type в обох напрямках, інваріант Σ рівнів тримається; (10) cross-invariant Σ level2 == parent l1 assert-guard пройшов усі 11 днів; (11) MIN/MAX date agg без поля у columns — response.aggregations[i].type='date' (Bug #620 regression-guard); (12) SQL-семантика AVG grand = SUM/COUNT а не avg-of-avg; (13) nullable date group → 1 `∅`-group зі 100% суми; (14) `@db.Date` (documentDate) Kyiv-конверсія стабільна (00:00Z+3=03:00 same day); (15) boolean groupBy ("true"/"false"); (16) UI-присутність — AGG_SHORT (Σ/К-сть/сер./мін./макс.), legend, collapse, 8 ENUM_LABELS у 4 usage-сайтах. Report Builder feedback фікси перевірені, регресії немає. Baseline: API 1151/1151, Web 495/495, TSC 0/0. Попереднє: 2026-09-03 Report Builder pivot cycle 3 (live) — 2 HIGH fixes (Bug #619 SUM(stockMovement.quantity) включав RESERVATION/RESERVATION_RELEASE як фізичні → фантомні −20; тепер non-physical types виключено, +37→+57. Bug #620 MIN/MAX date без поля у columns рендерився як гроші «1 780 963 200 000,00 грн»; збагачено response.aggregations типом+label, fmtAggValue/aggAliasLabel беруть тип звідти першим). Commit 5d79db82.
Конструктор звітів LIVE-ПЕРЕВІРЕНО (2026-09-03, docker піднято): міграція 20260903120000_add_saved_reports застосована; групування контрагент→товар + інваріант Σтоп==grandTotal (3700==3700), enum-relation (Контрагент.Тип), фільтр status=COMPLETED, save/runSaved, валідації (invalid enum→400, groupBy>5→400) — усе ✓. Фікс під час верифікації: прибрано `where` з nested include (to-one relation → Prisma "Unknown argument where"). UI рендериться (палітра+3 зони). Native HTML5 drag Playwright не симулює (обмеження PW, не баг). Лишилось: QA-ланцюжок sync→review→tester.
Latest feature: 2026-09-03 (feat/supplier-payments) — «Конструктор звітів (Report Builder)». Вкладка «Конструктор» на /reports: drag полів у колонки/групування(≤5)/фільтри, поля зв'язків (Контрагент.Тип), підсумки SUM/COUNT/AVG/MIN/MAX, збереження (SavedReport), експорт CSV/XLSX. Backend: метадата-реєстр (report-registry.ts) — ЄДИНЕ джерело правди; безпечний Prisma-білдер (whitelist + hasOwnProperty-guard, injection неможливий); JS-групування ≤5 рівнів. 9 сутностей: WorkOrder/WorkOrderPart/PurchaseOrderLine + Invoice/Payment/SettlementTransaction/StockMovement/StockBatch/StockItem (реєстр розширюваний, движок не міняється). Профілі FULL vs APPEND_ONLY (Payment/StockMovement/SettlementTx/StockBatch — без deletedAt); StockMovement.quantity знакова (signedByType). Повний QA-ланцюжок пройдено (sync 3 / review 4 вкл. a11y / tester 2 вкл. Bug #617 CRITICAL Prisma include+select). Live: інваріант Σлистків==grandTotal у 14+ сценаріях diff=0, знак WRITEOFF−/RECEIPT+ (нетто 37). Коміти b6301d0e/320e1e0a/463da2ad/0fb4d368/a29ae594/13861792/ea9d693d. API 1112→1139, Web 491→495.
3 повні QA-цикли завершено (2026-09-02, feat/supplier-payments): cycle 1 — 1 CRITICAL (AVG_COST sentinel) + altitude ''→null; cycle 2 — 1 HIGH (Bug #613 concurrency) + 3-рівневий backstop (pre-check→post-upsert→DB CHECK) + no-tx self-wrap; cycle 3 — 0 findings (КОНВЕРГЕНЦІЯ + empirical live-DB evidence: CHECK кидає 23514, Σremaining==quantity 0-mismatch 8 пар, balance==Σsigned 0-mismatch 222 акаунти). Кожен цикл: sync→review→tester→optimize→e2e→simplify→code-review→security. API 1057→1095 (+38 guard). 19 комітів.
Повний QA-цикл 1 (2026-09-02, feat/supplier-payments): sync 0 / review 1 CRITICAL (AVG_COST sentinel) / tester 0 активних +24 guard / optimize 2 (consume index + PO memo) / e2e 1 (stale SUPPLIER_PAYMENT sign) / simplify → sentinel batchId ''→null у джерелі + reuse BALANCE_SIGN / code-review 0 / security 0. Altitude усунув in-band '' sentinel у @db.Uuid (тип string|null).
Повний QA-цикл 2 (2026-09-02): sync 0 / review 0 / tester 1 HIGH (Bug #613 concurrent WRITEOFF race → від'ємні залишки без row-lock) +5 guard / optimize 2 (single-pass agg) + верифікував concurrency-фікс НЕ додає RTT / e2e 59/59 / simplify → DB-CHECK backstop (stock_items/stock_batches non-neg, міграція 20260902210000) + sumLineTotals у vat.ts + гейт guard / code-review → createMovement no-tx self-wrap у $transaction (orphan-BatchConsumption gap) / security 0 + orgId defense-in-depth. Ключове: інваріант не-відʼємності на 3 рівнях (pre-check → post-upsert throw → DB CHECK); atomicity через self-wrap.
Повний QA-цикл 3 фінальний (2026-09-02): review 0 findings — КОНВЕРГЕНЦІЯ. Незалежний фінальний прохід підтвердив: (a) createMovement no-tx self-wrap — recursive виклик з `innerTx` skip-ає wrapper на 2-му проходженні (без подвійної обгортки, 15s timeout адекватний); всі `db.*` у гарячій гілці; `getAvgCost` через `this.prisma` як read-only snapshot ДО списання — семантично коректно. (b) DB CHECK міграція 20260902210000 — clamp idempotent, DO-guard на pg_constraint idempotent, імена унікальні (grep migrations/ = 1 match), `isActive=false` при `remainingQty<0` — логічно (broken batch → deactivate). (c) sumLineTotals `unknown`-типізація приймає Prisma.Decimal і plain number; обидва call-sites (invoices.clone line 338, refreshFromWorkOrder line 698) передають об'єкти з 3 полями. (d) Cross-cutting: 0 `React.X`, 0 `console.log`, 0 `: any`, 0 BOM, всі findMany мають `take`, всі $transaction мають timeout, 0 hard-delete у зачеплених сервісах, 0 sentinel/orphan patterns. Baseline тримається: API 1095/1095, Web 488/488, TSC api ✅ 0 / web ✅ 0. Знахідки не з цієї сесії (pre-existing SUGGESTION, залишено на майбутнє): `fetchPartCoefficients` в work-orders.service:1559 робить `goodUoM.findMany({where:{id:{in:uomIds}}})` без orgId — defense-in-depth gap (UUID PK, атака неможлива).
Latest optimize: 2026-09-02 (/sto-optimize cycle 2, HEAD 33008171, feat/supplier-payments — post-concurrency-фікс аудит Bug #613) — 2 знайдено, 2 виправлено. Концепційне з'ясування ключового питання циклу: (a) InventoryService.createMovement post-check `.select({quantity, reserved})` на upsert — це RETURNING projection, НЕ окремий RTT (Prisma компілює у UPDATE ... RETURNING одним statement). Тестер писав "додатковий round-trip" у commit message — фактично помилка concept; guard залишений як є. (b) BatchService.consumeBatch updateMany `{where:{id, remainingQty:{gte:take}}}` — PK lookup O(1), НЕ використовує covering index циклу 1 (і не мусить). Race-safe безкоштовно (count у response). Обидва concurrency-захисти правильні у своєму місці — НЕ ослаблені. Реальні знахідки в не-hot-path aggregation: (1) supplier-payments.getSchedule.suppliers.map: `Object.values(byDate).reduce()` на кожного постачальника → накопичити byDateSum inline у циклі що заповнює byDate. 50 suppliers × ~20 dates: 50 alloc + 50 reduce-closures → 0 alloc. (2) invoices.service (2 місця: clone + updateFromWO): три reduce підряд по lineData (totalWithoutVat/totalVat/totalWithVat) → single-pass for-of з 3 accumulators. 100-line invoice: 300 iter + 3 closures → 100 iter + 0. Baseline тримається: API vitest 1093/1093, Web vitest 488/488, TSC api/web ✅ 0. SKILL.md +4 patterns: (a) concurrency-guard `.select` анти-паттерн (не оптимізуй те що не зламане, зрозуміти returning-projection); (b) conditional-decrement через updateMany + gte + count-check; (c) `Object.values.reduce` після filter-loop → merge sum у build-loop; (d) three-reduce → single-pass for-of. Попереднє: 2026-09-02 (/sto-optimize cycle 1, HEAD 1350cb3f, feat/supplier-payments — фокус на нещодавніх FIFO/COGS-змінах) — 2 знайдено, 2 виправлено. Backend/DB (1): `stock_batches (orgId, goodId, warehouseId, isActive, createdAt)` — covering B-tree index для BatchService.consumeBatch (FIFO/LIFO). Існуючий `(orgId, goodId, warehouseId, isActive)` покриває WHERE-префікс, але Postgres робив external sort на кожній сторінці PAGE=100 партій. Викликається на КОЖНОМУ WRITEOFF / WO-COMPLETED / TRANSFER-out — sustained CPU під розхід. Тепер index-order scan asc/desc без сортування. FEFO навмисно пропущений (рідкісний cost method, write-cost на INSERT переважає). Migration 20260902200000 + `@@index` у schema.prisma. Frontend (1): purchase-orders/page.tsx — `nowMs = useMemo(() => today?.getTime() ?? 0, [today])` замість `today?.getTime() ?? 0` inline у КОЖНОМУ рядку .map() (20-50 PO × 2 місця = 40-100 calls per render). Primitive number — memo-friendly для ExpiryBadge. Огляд hot-path (без нових знахідок, підтверджено чисто): (a) createMovement — `resolveCostMethod` gate за quantityDelta<0 (правильно), settings кеш Redis-hit; (b) consumeBatch while-пагінація з `progressed` guard OK, sentinel batchId='' truthy-guard у createMovement + WO writeOff уже додано commit 184b257a; (c) getSchedule single-pass totals + hot-path indexes уже додано cycle 2026-08-30 (counterparty_contracts + supplier_payments); Promise.all для 3-х initial queries; (d) TRANSFER sequential (writeoff→receipt) виправдано src.weightedCostPrice→target.price dependency — коментар пояснює; (e) SettlementsTabContent Promise.all для balance+txs+acts уже; (f) SupplierPaymentScheduleTab чистий (useQuery-driven). Baseline тримається: TS api ✅ 0 | web ✅ 0, тести inventory + supplier-payments 193/193. Попереднє: 2026-08-30 (FULL /sto-optimize, HEAD 50f50cc6, feat/supplier-payments, цикл 3/3 фінальний) — 8 знайдено, 8 виправлено (усі sibling-drift того самого патерну з циклів 1-2, без DB). Backend (7): INV_SORT_FIELDS/SD_SORT_FIELDS/PO_SORT_FIELDS/WO_SORT_FIELDS — sort whitelists у body findAll → module-level (all 4 CRUD services with sort); BALANCE_SIGN у settlements.createTransaction (hot-path FSM PAID/CONFIRMED) → module-level; SLOT_INCLUDE + CONFLICT_SELECT у calendar (Prisma include/select shapes) → module-level (create/update slots + public throttled checkConflicts). Frontend (2): EMPTY_WORK_ROW+EMPTY_GOOD_ROW у ServicesTab body (typing → re-alloc per render) → module-level; WORK_END_MIN у calcEndAt helper (recomputed на кожен keystroke у «Тривалість») → module-level. TS: api ✅ 0 | web ✅ 0. Vitest: 996/996 API + 488/488 Web (0 regressions). SKILL.md +1 meta-pattern «Sibling-drift audit protocol» — після кожного perf-fix треба одразу extend grep на ВСІ sibling-модулі того ж шару (не тільки git-diff scope). Cycle 2 попереднє: 2026-08-30 (HEAD 5858213e) — 4 знайдено, 4 виправлено. (1) supplier-payments.service.findAll: `SORT_FIELDS: Record<string,string>` декларувався у тілі → module-level `SP_SORT_FIELDS`. Викликається під polling 30s + filter/pagination. (2) getSchedule.totals: 3 послідовні `suppliers.reduce()` + `for (dates) { suppliers.reduce() }` → single-pass for-of з локальними акумуляторами (overdue/planned/total + inner for-in по byDate). 50 suppliers × 20 dates: 1150 iter → ~500 iter, 23 reduce-closure → 0. (3) purchase-orders/page.tsx: UUID_RE regex + `statuses` tuple-array з тіла компонента → module-level `PO_UUID_RE` + `PO_STATUS_FILTER_OPTIONS`. Alloc на кожен render, використовуються один раз (effect on mount) або статично (Filter chips). (4) supplier-payments/page.tsx: `const SORTABLE: Record` декларувався INSIDE `visibleColumns.map()` callback → module-level `SP_SORTABLE_BY_COL`. N-кратна амплітуда (alloc для КОЖНОЇ visible column на КОЖНИЙ render). SKILL.md +2 patterns («sort-field whitelist inside body / inside .map», «multi-scan reduce aggregation extend of twin-scan»). TS: api ✅ 0 | web ✅ 0. Vitest: 992/992 + 488/488. Cycle 1 попереднє: 2026-08-30 (HEAD a5fc685e) — 3 знайдено, 3 виправлено: (1) DB index counterparty_contracts(orgId, contractType, deletedAt); (2) DB index supplier_payments(orgId, purchaseOrderId, deletedAt); (3) purchase-orders.receive Array.find→Map bucket. Migration 20260830120000.
Latest feature: 2026-09-02 (feat/supplier-payments) — «Drill-down документів у графіку оплат». Клік по клітинці/колонці шахматки «Графіка оплат» → панель під таблицею зі списком PO (№·дата·сума·залишок·перехід на PO через PurchaseOrderCreateModal). Тригери: постачальник×день, рядок «Разом» (усі постачальники, supplierId опційний), бакети overdue/planned. Backend: винесено ЄДИНИЙ private computeScheduleAllocations (FIFO-налив боргу на PO + кредит-ліміт з per-PO алокаціями) — getSchedule сумує у бакети, getScheduleDocuments фільтрує по бакету → Σ allocated панелі == сума клітинки ЗА КОНСТРУКЦІЄЮ (немає sibling-drift, live-verified 18/18 клітинок). Новий GET /supplier-payments/schedule/documents (date XOR target, supplierId?) перед :id. Frontend: useSupplierPaymentDocuments (enabled лише при кліку), клікабельні клітинки з сумою, панель drill-down. QA: sync 0 / review 1 IMPORTANT (a11y role=button без tabIndex+onKeyDown, WCAG 2.1.1 → activateOnKey+focus-ring) / tester Bug #616 MEDIUM (date DTO без @IsDateString strict → silent-empty, sibling-drift Bug #595) fixed. API 1106→1112, коміти 2d960bc9+c7708711+4ff47b9d. Попереднє: 2026-08-31 «FIFO-графік оплат по документах + колонки оплати у списку купівлі». РЕФАКТОР логіки графіка: пропорційне масштабування балансу → FIFO-налив. Реальний борг (SettlementAccount.balance, |−balance|) «наливається» на непогашені RECEIVED/PARTIAL PO по черзі від найстарішого (orderBy paymentDate asc nulls first); кожен PO лягає у свою колонку (overdue/byDate/planned) з РЕАЛЬНИМ залишком; PO, до яких борг не дійшов = оплачені (не показуються); надлишок понад ΣPO → overdue. Прибрало мікро-частки масштабування (45.55/1.45) — тепер осмислені суми документів. Підсумок = balance (узгоджено зі звітом ДЛЯ активних SUPPLIER/BOTH counterparty — Bug #599/#600). Список купівлі /purchase-orders: +колонка «Дата оплати» (сортовна, nulls last) + «Днів до оплати» (ExpiryBadge «N дн.»/«Прострочено N дн.», лише де outstanding>0). Backend: findAll += groupBy CONFIRMED-платежів → outstanding у PO list DTO; PO_SORT_FIELDS/DTO whitelist += paymentDate; buildSortOrderBy += nullableFields option (nulls:last). Live: FDGD −1600 → графік 1600; DESC-sort дати зверху null внизу; CLIENT-type відфільтрований. Коміти 282d5fba+05ebbeb1+441c04aa. Попереднє (foundation): 2026-08-29 «Графік оплат постачальникам». PurchaseOrder.paymentDate (нова колонка, авто-fill у receive() = сьогодні+paymentDeferDays договору; редаговане поле в PO-модалці). GET /supplier-payments/schedule — шахматка боргів по датах (RECEIVED/PARTIAL PO, outstanding=totalAmount−ΣCONFIRMED payments, bucket overdue/дата/planned, кредит-ліміт з найпізніших). Вкладка «Список/Графік оплат» на /supplier-payments (SupplierPaymentScheduleTab). Tests: +5 getSchedule spec, +2 PO receive auto-fill, +1 E2E schedule tab. Live: today+10 auto-fill ✓, 5000−2000ліміт=3000 ✓.
Sync:       2026-09-03 ✅ Dir1 1 fixed (useRunSavedReport hook) | Dir2 1 fixed (op=in у FilterZone) | Dir3 1 fixed (SavedReport.createdBy optional)
Latest review: 2026-09-06 (auto, Phase D пов'язані документи StockDocument/SupplierReturn, HEAD 8db8a589, main) — **1 IMPORTANT знайдено/виправлено**. Огляд schema 53c0e7b0 (nullable FK purchaseOrderId, idempotent migration, covering-індекси — ✓ additive/safe), backend 8d898757 (getLinkedDocuments/getLinkedCounts, DTO LinkedCountsDto, route ordering), create-picker 6bd007f6 (EntityPickerField + SearchPickerModal, dirty-deps, create-only, savingRef guards — усе ✓). ЗНАЙДЕНО: getLinkedCounts у ОБОХ модулях безумовно рахував PO/склад(и)/постачальника за наявністю FK, тоді як getLinkedDocuments фільтрує deletedAt:null (FK ON DELETE SET NULL спрацьовує лише при hard-delete, тож soft-deleted реф лишає FK). → soft-deleted PO/склад/постачальник давав badge «1» + порожню панель (Bug #A/#641 count!=detail, той самий урок що invoices.getLinkedCounts вже кодифікує). Fix: findMany живих id → Set-membership перед інкрементом (дзеркалить invoices), +2 spec-кейси (soft-deleted→count=0). Решта чек-листа чиста: orgId+deletedAt:null скрізь, Decimal→Number, zero-init counts, {} на empty, route :id/linked-documents перед :id, opt FK-guard skip при undefined + tenant-scoped, edit-path не персистить PO. TS: api ✅ 0 | web ✅ 0. Tests: API 82/82 (stock-documents+supplier-returns), Web 40/40 (dirty-guard 4/4). SKILL.md вже має Bug #A/#641 lesson у §6/§13 (getLinkedCounts liveness) — новий grep-детектор не потрібен, урок кодифіковано. Попереднє: 2026-09-02 (auto, drill-down документів графіка оплат, HEAD c7708711, feat/supplier-payments) — **1 IMPORTANT знайдено/виправлено (a11y)**. Огляд коміту 2d960bc9 «drill-down документів у графіку оплат»: computeScheduleAllocations єдине джерело правди — сум панелі і клітинки збігаються за конструкцією (verified by service.spec 48/48 + explicit консистентність-тест setup×3); tenant isolation orgId у всіх 3 запитах helper-а (+cross-tenant guard supplierId у findAll); синтетичний борг без документа poId='' коректно неклікабельний на фронті (clickable=!!doc.poId); панель має text-right/tabular-nums/fmtMoney; role="button" лише коли sum>0.005. Проблема: клітинки role="button" не мали tabIndex+onKeyDown (WCAG 2.1.1 fail — keyboard-only юзер бачить фокус, Enter/Space не активують); той самий gap у <tr onClick> рядка панелі. Fix: activateOnKey helper (Enter/Space preventDefault→onClick), +tabIndex={0} +role/tabIndex conditional, +focus-visible ring, aria-label для tr. Дзеркалить існуючий inline-edit-cell.tsx патерн. TS: api ✅ 0 | web ✅ 0. Tests: API 1106/1106, Web hook 13/13, Web loop 491/491. SKILL.md pattern «role="button" без tabIndex+onKeyDown — keyboard-only broken» доданий у §14 (див. Накопичені підходи). Попереднє: 2026-09-02 (auto, cycle 3 фінальний, HEAD 3f3a0a32, feat/supplier-payments) — **0 findings, конвергенція**. Незалежний прохід перевірив всі 4 фокус-питання (self-wrap createMovement, DB-CHECK migration, sumLineTotals `unknown`, повний чек-лист по diff 74bf41b6..HEAD = 26 файлів). Жодне з попередніх виправлень (циклів 1-2) не внесло нових проблем. Всі cross-cutting greps 0. Baseline: API 1095/Web 488/TSC 0/0. Попереднє: 2026-09-02 (auto, cycle 1, HEAD 74bf41b6, feat/supplier-payments — FIFO/COGS + supplier settlement types + PATTERNS мета) — 1 CRITICAL знайдено/виправлено. AVG_COST sentinel batchId='' пробивав UUID FK: `BatchService.consumeBatch(AVG_COST)` повертає `[{batchId:'', quantity, costPrice: avgCost}]` як позначку "агрегат"; `InventoryService.createMovement` після consume мав `if (consumed.length===1) db.stockMovement.update({data:{batchId: consumed[0].batchId}})` — пише '' у колонку `batchId String? @db.Uuid` → Postgres runtime "invalid input syntax for type uuid: """ → WRITEOFF/WO COMPLETED падав на всіх org з costMethod=AVG_COST. Той самий баг у `WorkOrdersService.writeOffPartsAndCharge`: `batchId: writeoff.consumed.length===1 ? writeoff.consumed[0].batchId : null` → писало '' у WorkOrderPart.batchId. Fix: truthy-guard (`consumed[0].batchId ? ... : null`) в обох місцях + 2 regression-guard specs (mock sentinel batchId='' → update НЕ викликається / part.batchId=null). Tests: 1057→1059 API, TS: api ✅ 0 | web ✅ 0. SKILL.md +1 pattern «Sentinel empty-string у UUID FK колонку». Попереднє: 2026-09-02 (auto, HEAD 19f81ccb, feat/supplier-payments — FIFO/COGS фінансова зміна) — 0 findings (Critical: 0, Important: 0, Suggestion: 0). Огляд коміту 19f81ccb «підключення партійного FIFO-списання + COGS до розходів»: createMovement (void→CreateMovementResult), consumeBatch (take:100→while-пагінація), work-orders writeOff (batchCostPrice/batchId fixation, RELEASE→WRITEOFF ordering fix), stock-documents TRANSFER (послідовно, cost-carry src.weightedCostPrice), reconcile-міграція (FIFO-доспоживання надлишку, ідемпотентна). Перевірено: (1) consume-логіка — gate `available` кидає ДО будь-яких записів; upsert StockItem виконується у тій самій tx ПЕРЕД consume → інваріант Σ remainingQty==quantity зберігається за конструкцією; RESERVATION/RESERVATION_RELEASE/RECEIPT не викликають consume (quantityDelta умова). (2) AVG_COST — weightedCostPrice=getAvgCost ДО, фізичний декремент FIFO (щоб remainingQty спадав, не ламає інваріант); getAvgCost вживає this.prisma не tx — семантично коректно "snapshot ДО списання" (edge-case: 2 частини з тим самим goodId у одній tx матимуть stale avg, але per-part AVG — інгерентно snapshot, різні goodId у практиці). (3) weightedFromConsumed — `totalQty <= 0 → null` захист. (4) consumeBatch while-пагінація — `progressed` guard + `batches.length<PAGE` break; нескінченного циклу немає (partial batch re-fetch природній для FIFO/costMethod-порядку); throw `Недостатньо партій` при remaining>0. (5) work-orders writeOff — batchCostPrice fixation лише коли weightedCostPrice!=null; batchId=NULL при span; порядок RESERVATION_RELEASE→WRITEOFF коректний (гейт available спрацював би на 0 без RELEASE); documentLineId=part.id. (6) stock-documents TRANSFER — послідовно writeoff→receipt однієї лінії (не Promise.all: src.weightedCostPrice потрібен для target price); non-TRANSFER гілки з Promise.all(createMovement, maybeUpdateUom) не зачеплені. (7) resolveCostMethod — try/catch → FIFO fallback (offline-safe); getOrganisationSettings Redis-кешовано. (8) circular DI — Inventory→Settings→ExchangeRates, Settings не залежить від Inventory ✅. (9) reconcile-міграція — LEFT JOIN коректний (soft-deleted stockItem→deficit=batch_sum→FIFO-споживання всіх партій — коректно); ідемпотентна (deficit=0→CONTINUE); no-op на чистій dev-БД; NOT NULL batch_consumptions поля (documentId=b.id workaround); gen_random_uuid ✅. (10) tenant isolation orgId + soft-delete у всіх запитах + $transaction timeouts (10s/15s). Тести: 4 нові у inventory.service.spec (consumeBatch з costMethod, weighted assertion, single-batch batchId, AVG_COST FIFO-декремент, RECEIPT не викликає consume) — сигнатура моків узгоджена з новою прикладу з SettingsService. TS: api ✅ 0 | web ✅ 0. Live-verified: RECEIPT 10×100+10×120 avg=110, WRITEOFF 15 FIFO span (10+5) COGS=1600 weighted=106.67, StockItem.quantity=5==Σremaining=5 ✅. Попереднє: 2026-09-02 (HEAD 484f6b92) — 1 finding (IMPORTANT), fixed 1/1. Огляд коміту 23ce9109 «виправлення знаку балансу постачальника» (schema enum += SUPPLIER_CHARGE/PAYMENT/REFUND, 2 міграції, 3 writer'и, BALANCE_SIGN exported, reconciliation act пере-використав axis). Financial+migration change — глибокий чек. Все ✓ у зачепленому scope (знаки коректні, міграція enum окрема від backfill, CASE у backfill дзеркалить BALANCE_SIGN, ідемпотентно re-type по строгих documentType, клієнтські txs недоторкані, немає circular DI, dashboard PAYMENT self-heals після backfill, reports self-heals бо агрегує by balance sign, sync не торкається SettlementTransaction). ЗНАЙДЕНО sibling-drift у не-diff scope: `apps/web/src/app/(app)/counterparties/[id]/PageClient.tsx:1331,1336` — вкладка «Розрахунки» у картці CP мала ІДЕНТИЧНУ inline-копію старої осі `['PAYMENT','PREPAYMENT','REFUND','CREDIT_NOTE'].includes(t.type)` → SUPPLIER_CHARGE (=−1) НЕ в списку → малювався як «+ red» (наче нам винні), SUPPLIER_PAYMENT/REFUND правильний знак але без success-кольору. Fix: 2 module-level Set (BALANCE_UP_TX_TYPES/CHARGE_LIKE_TX_TYPES) + коментар-посилання на BALANCE_SIGN. Патерн: dual-consumer axis drift — при розширенні enum-у знайти ВСІ inline TX_TYPES списки grep (не тільки основний файл scope). SKILL.md +1 pattern «Sibling-drift for enum axis: TX_TYPES»; після 3-го consumer → shared helper. TS: web ✅ 0 errors. Vitest settlements: 23/23 pass. Попереднє: 2026-09-01 (HEAD de2d692e) — 1 finding (SUGGESTION), fixed 1/1. Огляд коміту ee23c53a «name-обов'язковість (гнучко)». Backend hasCounterpartyName + PATCH-merged guard + update-select розширено на 3 name-поля — коректно; всі 48 counterparties spec-тестів PASS (жоден не створює CP без name; SUPPLIER=companyName, CLIENT/BOTH=firstName). Frontend CounterpartyEditModal guard+disabled+required=SUPPLIER — коректно. НОВЕ (третій path): CalendarSlotModal wizard step-1 «Нового клієнта» мав власний guard `!newCp.firstName && !newCp.lastName && !newCp.companyName` — без .trim() і з іншим повідомленням. Whitespace-only «   » → frontend truthy pass → backend 400 без inline-помилки. Fix: три `.trim()` + повідомлення дзеркалить бек+CounterpartyEditModal. TS: api 0 | web 0. Vitest: 488/488 web (0 regressions). Patterns: dual-path input guard consistency — при cross-field валідації на беку треба знайти ВСІ frontend-entry-points (grep POST /counterparties → знайдено 2 модалі) і синхронізувати guard-логіку + повідомлення. Попереднє: 2026-08-30 (HEAD e02c1288, feat/supplier-payments, цикл 3/3) — full-project pass: 2 SUGGESTION cleanups, 0 correctness bugs. (1) 6 API файлів дублювали власний `UUID_RE` (booking.controller, goods.controller, goods.dto, warehouses.dto, works.dto, zones.dto) → консолідовано на `UUID_REGEX` з `@sto/shared` (single source of truth, дзеркалить cycle-2 web-side консолідацію). (2) `calendar.utils.displayCounterparty` → proxy до `displayCounterpartyName` з `lib/utils` (виправляє latent bug коли companyName=""/whitespace — `??` пропускав, треба `.trim()`). TS 0 errors (api+web+shared), vitest 992/992 API + 488/488 Web. Cycle 2 попереднє: 2026-08-30 (HEAD 4fcc1ab4) — 0 нових. Cycle 1: 2026-08-29 (HEAD ad65c36f) — 5 issues (schedule + PO paymentDate), fixed 5/5.
Latest tester: 2026-09-02 (/sto-tester drill-down документів графіка оплат, HEAD 6c8eeff5, feat/supplier-payments) — **1 MEDIUM знайдено/виправлено (Bug #616)**. Живий audit фічі «drill-down документів у графіку оплат» (коміти 2d960bc9 + c7708711). Головне: **КОНСИСТЕНТНІСТЬ-ІНВАРІАНТ ТРИМАЄТЬСЯ ✅**. Автоматизований probe-скрипт (`scratchpad/invariant.py`) виконав 18 перевірок: для КОЖНОЇ ненульової клітинки шахматки (7 постачальників × per-cell overdue+byDate+planned + totals row) `Σ allocated з /schedule/documents == значення клітинки з /schedule` — **18/18 checked, 0 fails**. Кредит-лімітний edge (АвтоДеталь ТОВ: bal=−49683, limit=2000, schedule.total=47683 = 49683−2000) — інваріант тримається; документи показують зменшений allocated з правильним ранжуванням planned→byDate-desc→overdue. Синтетичний «борг без документа» (poId='') 1 рядок (АвтоДеталь ТОВ, alloc=1030) включений у overdue, Σ сходиться. Валідаційні edge-cases 8/8: date+target=400 ✅, ні один=400 ✅, invalid supplierId non-UUID=400 ✅, вікно>100днів=400 ✅, from>to=400 ✅, invalid target value=400 ✅, no auth=401 ✅, from missing=400 ✅. **Bug #616 MEDIUM**: `SupplierPaymentScheduleDocumentsQueryDto.date` мав тільки `@Matches(YMD_RE)` без `@IsDateString({strict:true})` → семантично-невалідні дати `2026-99-99`/`2026-13-01`/`2026-02-31`/`9999-99-99` пропускалися як 200 з empty array замість 400 (regex перевіряє лише shape, не parseability). Це sibling-drift паттерну Bug #595 у ТОМУ Ж DTO-файлі: fix застосований до `from`/`to`, doc-comment описує пастку, але новий field `date` (доданий у drill-down) забув парну декорацію. Fix: `@IsDateString({strict:true}) + @Matches(YMD_RE)` дзеркалить `from`/`to`. Regression-guard: +5 нових DTO-validation тестів у contract-spec (4 semantic-invalid dates → validation error + 1 valid date happy-path + symmetric guard на `SupplierPaymentScheduleQueryDto.from`). Live retest після фіксу: всі 4 bad dates → 400, happy path 200. Baseline тримається: API 1106→1112 (+6 регрес-guard), Web 491/491, TSC api/web ✅ 0. SKILL.md +1 sub-pattern «sibling-drift regex-shape validation у ТОМУ Ж DTO-файлі» + grep-guard awk-скрипт. Попереднє: 2026-09-02 (/sto-tester bug hunt ЦИКЛУ 3 фінальний, HEAD 47e26321, feat/supplier-payments — жива верифікація) — 0 активних багів. Ad-hoc probe-скрипт проти живої dev БД виконав 7 перевірок і всі 7 зелені: (1)(2)(3) `stock_items.quantity/reserved < 0` INSERT + UPDATE → 23514 `stock_items_quantity_nonneg` — DB CHECK живий на обидва DML; (4) `stock_batches.remainingQty < 0` INSERT → 23514 `stock_batches_remaining_nonneg`; (5) `$transaction(async tx => { create + $executeRaw INSERT з CHECK violation })` → tx rollback → StockMovement count незмінний → атомарність тримається на mixed ORM+raw шляху всередині tx (моделює цикл-2 createMovement self-wrap семантику); (6) system-wide інваріант `Σ remainingQty(active) == StockItem.quantity` для 8 живих пар — 0 mismatches (реальні партії від реальних RECEIPT/WRITEOFF операцій); (7) settlement invariant `balance == Σ signed(tx)` для 222 акаунтів (11 non-zero) — 0 mismatches (враховано повний sign-map з CHARGE+PAYMENT+PREPAYMENT+REFUND+CREDIT_NOTE+SUPPLIER_CHARGE+SUPPLIER_PAYMENT+SUPPLIER_REFUND). Regression guards зелені: `batch.invariants.spec` 29/29 + `settlements.invariants.spec` 13/13 + `supplier-payments.service.spec` 37/37 + `purchase-orders.service.spec` 45/45. Baseline тримається: API 1095/1095, Web 488/488, TSC api/web/shared ✅ 0. SKILL.md +1 pattern «Live-DB probe для DB CHECK/rollback/invariant верифікації» (probe-time semantic-map hardcoding = false-positive risk — читай мапу з коду, не переписуй). Ключовий висновок конвергенції: цикл 1 → semantic invariants, цикл 2 → operational invariants (concurrency, DB layer), цикл 3 → **live evidence** — механізми циклу 2 доведені empirically проти живої БД. Попереднє: 2026-09-02 (/sto-tester bug hunt ЦИКЛУ 2, HEAD e0385776, feat/supplier-payments — concurrent race + mid-life switch) — 2 real багів (HIGH: 1 concurrency, MEDIUM: 1 test-coverage) + 1 LOW documented. Bug #613 HIGH concurrent WRITEOFF race: pre-check `available>=|qty|` читає stale snapshot без row-lock, Postgres серіалізує write → від'ємний quantity у StockItem. Fix: post-upsert re-check `quantity>=0 && reserved>=0` (rollback у $tx) + `stockBatch.updateMany({where:remainingQty:{gte:take}})` conditional decrement (count=0 при race → throw). Bug #614 MEDIUM: +3 property-based mid-life switch invariants (FIFO→AVG→LIFO sequences). Bug #615 LOW: RECEIPT concurrent без row-lock — documented (append-only, не порушує інваріант). +10 нових regression-guards (3 inventory.service.spec race + 2 batch.service.spec updateMany + 5 batch.invariants.spec). API 1083→1093, Web 488 (без змін), TS 0/0. Ключовий висновок: цикл 1 покрив семантичні інваріанти (Σ, sign, FIFO order), цикл 2 покрив операційні інваріанти (race, mid-life, defensive DB layer). Попереднє: 2026-09-02 (/sto-tester bug hunt циклу 1, HEAD 184b257a, feat/supplier-payments — фінансові інваріанти) — 0 нових активних багів, +1 regression-guard (Bug #612): новий `apps/api/src/modules/inventory/batch.invariants.spec.ts` з 24 property-based тестами у 4 describe-блоках. **BatchService consume invariants** (10 тестів): Σ consumed==qty, масовий баланс, нема партій у мінус, remainingQty=0→isActive=false, FIFO/LIFO порядок, нестача→error БЕЗ мутації (all-or-nothing), AVG_COST sentinel форма (length===1, batchId==='' falsy), single-vs-span batchId fixation, cross-method Σ==qty. **SupplierPayments.getSchedule FIFO invariants** (5 тестів): Σ bucket==payable, надлишок→overdue, FIFO строгий порядок закриття PO, кредит-ліміт зменшення planned→dates-desc→overdue, ліміт≥payable→усе 0. **BALANCE_SIGN supplier cycle** (5 тестів): SUPPLIER_CHARGE→−X, повний цикл→0, payable=max(0,−balance), частковий цикл X−Y, SUPPLIER_REFUND має ТОЙ САМИЙ знак що SUPPLIER_PAYMENT (регресія). **StockDocument TRANSFER cost-carry** (4 тести): weightedCostPrice→target.price, null→fallback, **0 (free sample) через `??` НЕ падає у fallback** (документує `||` як БАГ). Всі 24 PASS з першого запуску — інваріанти тримаються. API 1059→1083, TS 0/0. Причина 0 активних багів: усі 5 фокус-областей уже покриті recent commits (#606-#611 покрили BALANCE/UI-tone/FIFO/COGS/TRANSFER; #597-#600 покрили FIFO schedule window/type-filter/soft-delete). Попереднє: 2026-09-02 (/sto-tester, HEAD 450e2a24, feat/supplier-payments — FIFO/COGS підключення commit 19f81ccb) — 3 баги (HIGH:2, MEDIUM:1), fixed 3/3. Живий audit ФІНАНСОВОЇ зміни (партійне списання FIFO/LIFO/FEFO/AVG_COST + COGS у WorkOrderPart.batchCostPrice + TRANSFER cost carry + reconcile-міграція). 14 curl-сценаріїв через API: (1) LIFO — WRITEOFF 5 бере найновіший @120 ✅; (2) FEFO з null-expiry → fallback createdAt asc ✅; (3) AVG_COST weighted=108.33, фізично FIFO ✅; (4) shortage guard — WRITEOFF>available кидає 400 ДО consume, партії/StockItem незмінні ✅; (5) TRANSFER single-batch cost carry — target batch costPrice=100 (не salePrice=200) ✅; (6) TRANSFER span 3+2 → weighted COGS=48 ✅; (7) WO COMPLETED single — batchCostPrice+batchId у part, StockMovement.batchId single-batch trace, BatchConsumption.documentLineId=part.id ✅; (8) WO span — batchCostPrice=weighted, batchId=NULL, 2 consumptions ✅; (9) RESERVATION НЕ чіпає партії ✅; (10) partial multi-writeoff однієї партії ✅; (11) supplier-return live консюмить партії FIFO ✅; (12) reconcile idempotent no-op на чистій ✅; (13) reconcile deficit — штучний +100 → FIFO доспоживає ✅; (14) глобальний інваріант Σremaining==StockItem.quantity: 0 mismatches ✅. Bugs #609-#611 — test-coverage gaps: **Bug #609 HIGH**: writeOffPartsAndCharge batchCostPrice/batchId writeback без regression-guard → +5 тестів у work-orders.service.spec (single/span/null/multi-parts/no-price-arg). **Bug #610 HIGH**: TRANSFER cost carry (src.weightedCostPrice → target.price) без regression-guard → +2 тести у stock-documents.service.spec (cost carry + fallback). **Bug #611 MEDIUM**: LIFO/FEFO/FIFO orderBy switch у consumeBatch без regression-guard → +3 тести у batch.service.spec (asc↔desc/nulls-last). API tests 1047→1057. TS 0/0. Live-verified: FIFO 15 span COGS=1600 weighted=106.67, StockItem.quantity=5==Σremaining=5 ✅. Попереднє: 2026-09-02 (HEAD d373c8c0, фінансова зміна знаку) — 3 баги (HIGH:1, MEDIUM:1, LOW:1), fixed 3/3. Baseline: API 1040/1040, Web 488/488, tsc 0/0, live invariant `balance == Σ BALANCE_SIGN(tx.type)×amount` — 139/139 counterparties ✅ (backfill ідемпотентний). Bug #606 MEDIUM: інверсія кольору balance-header у 2 UI-місцях (SettlementsTabContent >0=red|<0=success vs PageClient <0=red|>0=success) → після фіксу знаку постачальника обидва місця стали non-consistent: SUPPLIER balance=−48553 у /settlements=green (наче ОК), у /counterparties/[id]=red (правильно, ми винні). Fix: `settlementBalanceTone(balance, type)` + `settlementBalanceToneClass()` у `lib/utils.ts` — тип-aware (CLIENT>0=destructive/<0=warning; SUPPLIER<0=destructive/>0=warning; BOTH=nonzero→destructive). Обидва місця імпортують. Bug #607 LOW/cleanup: `reports.settlements()` не фільтрує `counterparty.deletedAt=null` → soft-deleted CP eaac0311 (SUPPLIER −700) показувався у звіті, розбіжність з schedule (7 vs 8 SUPPLIER, різниця 700). Fix: nested `where.counterparty = { deletedAt: null }` (Prisma.SettlementAccountWhereInput). Bug #608 HIGH: regression-guard gap — 0 тестів на `BALANCE_SIGN` exhaustiveness (Bug #606 root cause: інверсія знаку ключа не зловиться compile-time). Fix: +3 vitest у settlements.invariants: (1) `Object.values(SettlementTransactionType).forEach(...) + expect(BALANCE_SIGN.CHARGE).toBe(1)` × 8 явних експектацій; (2) property-based `SUPPLIER_CHARGE(X) − SUPPLIER_PAYMENT(Y) + SUPPLIER_REFUND(Z) → −(X−Y−Z)`; (3) BOTH-mix `CHARGE(client) + SUPPLIER_CHARGE(sup) → client−sup` (знаки не інтерферують). Live-verify: (a) partial receive 1×500 → balance −49053, повний recv → −49553, partial SP 500 → −49053, SR 500 → −48553 (baseline) ✓; (b) BOTH-CP PO receive −700 → schedule shows 700 overdue, report totalCredit=700 ✓; (c) 10 CLIENT-tx CHARGE, 0 SUPPLIER_* (CLIENT-регресія undisturbed) ✓; (d) soft-deleted eaac0311 після fix БІЛЬШЕ не у /reports/settlements ✓. TS: api ✅ 0 | web ✅ 0. Vitest: 1043/1043 API (+3) + 488/488 Web (0 regressions). SKILL.md +1 pattern «Financial migration audit protocol»: (1) live invariant sweep через API, (2) cross-source coherence report ↔ schedule, (3) frontend sibling-drift audit на sign-encoding, (4) exhaustiveness runtime-assert для BALANCE_SIGN, (5) end-to-end чек всього циклу через API, (6) client-regression proof (Counter tx types, no SUPPLIER_* on CLIENT). Попереднє: 2026-09-01 (/sto-tester, HEAD 03a93799, feat/supplier-payments — restore-endpoints) — 5 багів (HIGH: 3, MEDIUM: 2), fixed 5/5. Bug #601 HIGH: `vehicles.restore()` не перевіряє чи parent garage активний → orphan reference (гараж soft-deleted, авто «зомбі», не показується у findAll(counterpartyId=)). Fix: prep-check findFirst({id,orgId}) з select garage.counterparty; якщо `existing.customerGarage.deletedAt !== null` → BadRequestException. Bug #602 HIGH: `vehicles.restore()` не перевіряє parent counterparty — той самий pattern, grandparent chain (vehicle→garage→counterparty). Fix: same prep-check + перевірка `existing.customerGarage.counterparty.deletedAt !== null`. Bug #603 HIGH: `restoreContract()` асиметричний з `findContracts/create/update/removeContract` — усі мають parent-CP guard, restore пропускає → contract у limbo (deletedAt:null, cp.deletedAt:not-null). Fix: findFirst({id,orgId,deletedAt:null}) перед updateMany. Bug #604 MEDIUM: `counterparties.contract.spec.ts` serviceMock не містить `restoreContract` — новий endpoint отримав би `TypeError: undefined is not a function`. Fix: додано `restoreContract: vi.fn()`. Bug #605 MEDIUM regression-guard gap: 0 автотестів на нові restore-endpoints (grep 0 matches). Fix: +6 tests у counterparties.service.spec.ts (restoreContract Bug #603 guard + double-restore + orgId + isPrimary=false + DTO shape), +NEW vehicles.service.spec.ts з 0→13 tests (restore happy/double/deleted-garage/deleted-CP/cross-tenant, findAll(showDeleted) shape, remove() atomic), +1 test у counterparties.contract.spec.ts (POST /restore endpoint call+params). Live verify: garage-deleted→400, cp-deleted→400 (veh) / 404 (contract), happy-path→201. TS: api ✅ 0 | web ✅ 0. Vitest: 1035/1035 API (було 1015 → +20 нових). Попереднє: 2026-08-31 (HEAD 441c04aa, FIFO-графік) — 3 баги (Bugs #598-#600). — 3 баги (MEDIUM: 2, LOW: 1), fixed 3/3. Bug #598 MEDIUM: sortBy=paymentDate&desc виносив null-date PO наверх (Postgres NULLS FIRST для DESC) → buildSortOrderBy += nullableFields?: Set → для nullable-field { sort, nulls:'last' }; PO findAll передає Set(['paymentDate']). Live: DESC→дати зверху. Bug #599 MEDIUM: getSchedule включав CLIENT-типу counterparty з balance<0 як «постачальника до оплати» (semantic contamination, latent — demo-CLIENT soft-deleted маскував) → filter counterparty.type in [SUPPLIER,BOTH]. Bug #600 LOW: docstring getSchedule «завжди узгоджений зі звітом» неправда (divergence на deleted/CLIENT counterparty, звіт не фільтрує) → docstring переписаний як trade-off. +16 регрес-тестів (12 pagination nullable + 4 PO outstanding). Vitest 1015/1015. NB: агент впав на API-error до коміту → фікси верифіковані вручну + закомічені. Попереднє: 2026-08-30 (HEAD e02c1288, цикл 3/3) — 1 баг (MEDIUM: 1), Bug #597 getSchedule cross-field guards. Bug #597 MEDIUM: getSchedule() cross-field guards (from>to, windowDays>100) + totals.byDate single-pass aggregator (з optimize cycle 2) без regression-тестів → патерн Bug #416 (refactor може видалити guard/agg без падіння CI). Fix: +4 нових `it(...)` кейсів у supplier-payments.service.spec.ts (rejects.toThrow + not.toHaveBeenCalled + boundary case + 2-supplier byDate sum). Vitest: 996/996 (було 992). SKILL.md +1 pattern («Cross-field guard + single-pass agg без regression-test» family Bug #416). Попереднє: 2026-08-30 (HEAD d5d58af7, цикл 1/3) — 4 багів (HIGH: 3, MEDIUM: 1), fixed 4/4. Bug #592 HIGH: spec purchase-orders.service.spec.ts:834 порівнював impl-Kyiv-дату з test-UTC-дату → flaky 3h/day (assertion 2026-09-09 vs 2026-09-08 на кордоні Kyiv-північ). Fix: use kyivToday/addDaysKyiv у spec. Bug #593 HIGH: 4 web тести падали "No QueryClient set" після SP modal RQ migration (commit 7e6bfab9); PurchaseOrderCreateModal транзитивно рендерить SP → тест PO теж вражений. Fix: renderWithQueryClient helper у 2 test-файлах. Bug #594 HIGH: SP modal create-branch bypassed useCreateSupplierPayment hook → cache staleness 30s. Fix: import hook, replace raw apiFetch на createMut.mutateAsync. Bug #595 MEDIUM: SupplierPaymentScheduleQueryDto @Matches(YMD_RE) прийнвував invalid "2026-99-99" → NaN → silent empty. Fix: додано @IsDateString({strict:true}). SKILL.md +4 patterns (spec-vs-impl DST parity, QCProvider post-RQ-migration, partial-hook-migration, regex-vs-semantic date). Попереднє: Bug #590 HIGH useConfirmSupplierPayment invalidate counterparties; Bug #591 useSupplierPayments.test.tsx.
Latest E2E: 2026-07-03 (SupplierPayment) — новий `apps/web/e2e/supplier-payments.spec.ts` 7/7 passed. Покриває: рендер сторінки/фільтрів/модалки, create→таблиця→cleanup, FSM DRAFT→CONFIRMED пише settlement PAYMENT + баланс постачальника −amount + documentType=SupplierPayment у transactions + CONFIRMED не видаляється, guard BANK_ACCOUNT без bankAccountId→400, DRAFT→CANCELLED не пише settlement. Тест сам сідить cash register (currency+branch — seed не містить). Related specs (supplier-returns, crud-purchase-order) без регресій.
Dev-сервери: API ✅ :3000 | Web ✅ :3001 | Docker: запускати вручну
Останній commit: 2026-09-03 — 5d79db82 fix(tester): report-builder — Bug #619 SUM(quantity) фантом резервувань, Bug #620 date agg рендериться як гроші. Live-guard: (#619) POST /reports/builder/run stockMovement.quantity groupBy=[type] → grandTotal +37→+57 (фантомні −20 від RESERVATION/RESERVATION_RELEASE зникли, ці типи тепер виключені як не-фізичні через NON_PHYSICAL_MOVEMENT_TYPES у numericValue — див. inventory.service.ts:187-190 quantityDelta=0 для обох); (#620) response.aggregations збагачено {type,label} з entity.fields (новий ReportAggEnriched); frontend fmtAggValue+aggAliasLabel беруть тип звідти першим (fallback на cols) → MIN documentDate без documentDate у columns тепер рендериться fmtDate замість fmtMoney(1.78e12→«1 780 963 200 000,00 грн»). Живий інваріант Σ(листкові aggregates)==grandTotal тримається у 1/2/3-рівневих groupBy усіх 9 сутностей (workOrder/workOrderPart/purchaseOrderLine/invoice/payment/settlementTransaction/stockMovement/stockBatch/stockItem), regression +5 тестів (aggregator 3 #619 + service 2 #620), API 1148/1148 (baseline 1143), Web 495/495, tsc 0/0. Попередній: 0fb4d368 fix(review): Report Builder — named React imports (React.DragEvent/ReactNode → 'react' named) + Modal exit-анімація (прибрано зовнішній `{open && <Modal>}` — сам Modal керує mount/unmount) + a11y/UX (`<tr>` mав cursor-pointer hover без обробника — перенесено на inner <button> + aria-expanded + focus-visible outline) + perf (useMemo для paletteFields та usedKeys — уникає перебору fields на кожен drag-hover tick). Backend review: 0 findings (tenant orgId скрізь у run+SavedReport CRUD ✓; hasOwnProperty-guard у getEntity/assertEnumValue ✓; ключі Prisma лише з реєстрових літералів у setWherePath/mergeIncludePath ✓; runFindMany `as any` ізольовано + model з whitelist ✓; take:5000 cap + truncated-сигнал ✓; ArrayMaxSize(5) groupBy ✓; ParseUUIDPipe на всіх :id ✓; @Roles OWNER/ADMIN/ACCOUNTANT на кожному endpoint ✓; soft-delete тільки де hasSoftDelete ✓). API 1130/1130, Web 495/495, tsc 0/0. Попередній: 463da2ad fix(sync): align Report Builder frontend with backend contract (useRunSavedReport hook + op=in у FilterZone з масивом + SavedReport.createdBy optional). Попередній: 320e1e0a feat(report-builder): frontend конструктора звітів (вкладка Конструктор — палітра/зони/групована таблиця/експорт). Попередній: b6301d0e feat(report-builder) backend движок (реєстр+білдер+агрегатор+SavedReport, +18 тестів). Попередній ланцюг: 4ff47b9d fix(tester): Bug #616 — semantic-invalid `date` у drill-down DTO (регресія паттерну Bug #595). `SupplierPaymentScheduleDocumentsQueryDto.date` мав тільки `@Matches(YMD_RE)` (регекс лише shape) → `date=2026-99-99/13-01/02-31` пропускалися як 200 з empty array замість 400. Sibling-drift у ТОМУ Ж DTO-файлі: fix застосований до `from`/`to` (Bug #595), doc-comment описує пастку, але новий field `date` доданий пізніше без парної декорації. Fix: `@IsDateString({strict:true})` перед `@Matches(YMD_RE)` дзеркалить `from`/`to`. +6 regression-guard у contract-spec (плюс valid+symmetric guard на `from`). Live retest: 4/4 bad dates → 400, happy path 200. Консистентність-інваріант (Σ allocated == cell) — 18/18 клітинок ✅. API 1106→1112, Web 491/491, tsc 0/0. Попередній: c7708711 fix(review): a11y для drill-down клітинок графіка оплат (WCAG 2.1.1). ClickableCell/TotalCell + <tr> клікабельний рядок панелі отримали tabIndex+onKeyDown (Enter/Space з preventDefault) + focus-visible ring + aria-label. role="button" без keyboard handler = мертвий фокус для клавіатурного юзера. Дзеркалить inline-edit-cell.tsx. API 1106/Web 491/tsc 0/0. Попередній: 3f3a0a32 fix(security): orgId у consumeBatch updateMany — defense-in-depth (цикл 2 security-review; batch.id — UUID PK з orgId-scoped findMany, атака неможлива, але CLAUDE.md #6). Попередній: 8636a7db fix(review): createMovement самообгортається у $transaction без tx (цикл 2 code-review — latent orphan-BatchConsumption gap: multi-write без rollback якби майбутній caller викликав без tx; self-wrap дзеркалить consumeBatch). +2 regression. Попередній: 76a7d0fa refactor(simplify): DB-CHECK backstop для залишків + sumLineTotals + гейт guard (цикл 2 — altitude: CHECK stock_items(quantity/reserved>=0) + stock_batches(remainingQty>=0) міграція 20260902210000 як джерело-правди backstop; app-guard лишається для локалізації+CAS; reuse: sumLineTotals у vat.ts для 2 invoice-сайтів; гейт post-upsert check за знаком дельти). Попередній: 950cb5b9 docs(skills): +4 patterns у sto-optimize (concurrency-guard `.select` не додає RTT, conditional-decrement updateMany, Object.values.reduce merge у filter-loop, three-reduce single-pass). Попередній: 33008171 perf(optimize): single-pass sum accumulators for supplier-payments schedule + invoices totals (cycle 2). Аудит feat/supplier-payments post-concurrency-фікс. Concurrency-guard (createMovement post-check + consumeBatch updateMany) залишений недоторканим після concept-верифікації: `.select` на upsert — це RETURNING projection не окремий RTT; updateMany з PK+gte — O(1), race-safe безкоштовно. Реальні знахідки: (1) supplier-payments getSchedule map: `Object.values(byDate).reduce()` per supplier → накопичення byDateSum inline у filter-loop; (2) invoices.service 2 місця (clone + updateFromWO): три reduce підряд → single-pass for-of. Baseline тримається 1093/488/0. Попередній: cbebc2f5 fix(tester): Bug #613 concurrent WRITEOFF race guard + Bug #614 mid-life switch invariants (cycle 2). Bug #613 HIGH concurrency: `createMovement` pre-check `available >= |qty|` читає stale snapshot без row-lock — 2 concurrent WRITEOFF того самого товару обидва проходять, а Postgres row-lock UPDATE серіалізує запис → другий залишає `StockItem.quantity=-N` без сигналу (немає CHECK constraint). Аналогічно `consumeBatch` безумовним decrement дає `remainingQty=-N`. Fix: (a) post-upsert re-check `if (upserted.quantity<0||reserved<0) throw` через `.select({quantity,reserved})` → rollback усього $tx; (b) `stockBatch.update({decrement})` → `updateMany({where:{id,remainingQty:{gte:take}}})` — conditional Postgres decrement, count=0 при race → «Партію змінено іншою транзакцією». Bug #614 MEDIUM: +3 property-based тести для mid-life switch costMethod (FIFO→AVG_COST→LIFO) — модель контракту що AVG_COST commit path декрементує FIFO (рядок 227 InventoryService). Bug #615 LOW: RECEIPT concurrent без row-lock — задокументовано без коду (append-only без порушення інваріанту). Regression-guards: +3 у inventory.service.spec (WRITEOFF race throw / RESERVATION_RELEASE race throw / happy-path); +2 у batch.service.spec (updateMany правильний filter / count=0 throw); +5 у batch.invariants.spec (2 mid-life switch + 3 race-simulation property invariants). API tests 1083→1093 (+10). TS 0/0. SKILL.md samovдосконalення: додати «Concurrent pre-check → write без row-lock» pattern + «updateMany з gte-filter для conditional decrement». Попередній: 02559610 refactor(simplify): sentinel batchId ""→null у джерелі + reuse BALANCE_SIGN (цикл 1). Altitude-фікс від simplify-агентів: BatchConsumeResult.batchId string→string|null, consumeBatch(AVG_COST) повертає null замість in-band '' sentinel → null напряму лягає у nullable @db.Uuid, обидва гейти (inventory.service + work-orders.service) згорнуто у плоский тернар; batch.invariants.spec реюзає ЕКСПОРТОВАНУ BALANCE_SIGN замість локальної копії (інверсія знаку у прод впаде тут); прибрано impossible fc.pre + тавтологію. Попередній у циклі: 58e522ef fix(e2e) stale SUPPLIER_PAYMENT sign (balanceBefore+200, type SUPPLIER_PAYMENT). Попередній: 1350cb3f perf(optimize): batch consume hot-path index + PO list nowMs memo (cycle 1). Backend/DB: covering index `stock_batches(orgId,goodId,warehouseId,isActive,createdAt)` — усуває external sort на КОЖНІЙ сторінці PAGE=100 партій у FIFO/LIFO consumeBatch (hot-path кожен WRITEOFF/WO-COMPLETED/TRANSFER-out під polling-навантаженням). Frontend: nowMs=useMemo для 20-50 PO рядків × 2 місця (daysUntil+ExpiryBadge) — primitive-stable для memo. Migration 20260902200000 + `@@index` у schema.prisma для sync-drift detection. FEFO skipped (rare cost method, write-cost переважає). Tests 193/193 inventory+SP, TS 0/0. Попередній: 2027fa85 fix(tester): Bugs #609-#611 — regression-guards для FIFO/COGS підключення (commit 19f81ccb). Живий audit ФІНАНСОВОЇ зміни: партійне списання FIFO/LIFO/FEFO/AVG_COST через API + WO COMPLETED batchCostPrice writeback + TRANSFER cost carry + reconcile-міграція idempotency + deficit reconciliation + глобальний інваріант. 14 curl-сценаріїв усі ✅. Bugs #609-#611 — coverage gaps: (a) writeOffPartsAndCharge writeback без regression-guard → +5 тестів; (b) TRANSFER cost carry без regression-guard → +2 тести; (c) LIFO/FEFO/FIFO orderBy switch без regression-guard → +3 тести. API 1047→1057, TS 0/0. Попередній: d2ae2e7e fix(tester): Bugs #606-#608 — тип-aware settlementBalanceTone + reports soft-delete filter + BALANCE_SIGN exhaustive regression tests. Bug hunt комітів 23ce9109 (виправлення знаку постачальника) + 484f6b92 (review sibling-drift PageClient). Bug #606 MEDIUM: інверсія кольору balance-header у 2 UI-місцях (SettlementsTabContent >0=red|<0=success vs PageClient <0=red|>0=success — обидва tип-незалежні). Fix: `settlementBalanceTone(balance, type)` + `settlementBalanceToneClass()` у `lib/utils.ts`, тип-aware (CLIENT>0=destructive; SUPPLIER<0=destructive; BOTH=nonzero→destructive); обидва місця імпортують. Bug #607 LOW: `reports.settlements()` не фільтрує `counterparty.deletedAt=null` (розбіжність з schedule) → nested Prisma filter. Bug #608 HIGH regression-guard: 0 тестів на BALANCE_SIGN exhaustiveness → +3 vitest (Object.values(enum).forEach BALANCE_SIGN defined + явні expect кожного знаку, property-based partial cycle, BOTH-mix). TS 0/0, API 1043/1043 (+3), Web 488/488. Live invariant 139/139. Попередній: 484f6b92 fix(review): sibling-drift — counterparty detail page settlements tab не знав про SUPPLIER_CHARGE. Оглянуто 23ce9109 (виправлення знаку балансу постачальника). Знайдено 1 IMPORTANT sibling-drift: PageClient.tsx:1331,1336 мала inline-копію старої осі, без SUPPLIER_CHARGE у BALANCE_UP → малював отримання товару як «+ red». Fix: 2 module-level Set + коментар-посилання на BALANCE_SIGN. TS: web ✅ 0. Попередній: 23ce9109 fix(settlements): виправлення знаку балансу постачальника — графік оплат оживає (schema enum += SUPPLIER_CHARGE/PAYMENT/REFUND, 2 міграції ADD VALUE + backfill, BALANCE_SIGN exported з reconciliation act reuse, 3 writer'и: receive→SUPPLIER_CHARGE, SP confirm→SUPPLIER_PAYMENT, SR confirm→SUPPLIER_REFUND). Попередній: 9d61dfaa feat(counterparties): після створення картка лишається відкритою в edit-режимі (onSaved(cp,isNew); головний список при isNew→setEditingCp(created) замість закриття → вкладки Авто/Договори; edit-only споживачі ігнорують isNew). Review 0 findings, web tsc 0. Попередній: fd78f7e8 docs: назва обов'язкова. Попередній: de2d692e fix(review): CalendarSlotModal name-guard trim+повідомлення=бек (третій входовий пункт до POST /counterparties поза CounterpartyEditModal — власний guard був без trim і з іншим текстом; whitespace-only обходив frontend і викликав backend 400 без inline). Fix: three `.trim()` + message mirrors backend+EditModal. TS 0/0, vitest 488/488. Попередній: ee23c53a feat(counterparties): назва контрагента обов'язкова (гнучко — companyName АБО firstName/lastName, cross-field). Backend hasCounterpartyName() у create+update (PATCH-merged), Frontend guard+disabled+required=SUPPLIER. api+web tsc 0. Попередній: 8c047275 fix(tester): Bugs #601-#605 — restore-endpoints parent-chain guards + regression suite (для фічі c30c22bd «галка Показувати видалені + відновлення договорів/авто» + review-fix 03a93799). Bugs #601-#605: 3 HIGH orphan-refs (veh restore без garage-check, veh restore без CP-check, contract restore без CP-check) + 2 MEDIUM test-coverage (serviceMock missing method + zero regression-tests). Fix: parent-chain guards у vehicles.restore + counterparties.restoreContract, +20 нових vitest (NEW vehicles.service.spec.ts, +6 у counterparties.service.spec.ts, +1 contract-spec). Live verify all fixes. Vitest 1035/1035. Попередній: 2026-09-01 — review-fix для c30c22bd (галка+restore). 2 findings: (1) IMPORTANT skip-first-run refs не скидались на CP-switch → toggle-useEffect давав дубль-запит (main + toggle обидва fetch-ать при зміні counterparty?.id). Fix: у головному useEffect `vehiclesLoadedRef.current=false; contractsLoadedRef.current=false;` + оголошення refs перед useEffect (уникнення subtle TDZ). (2) SUGGESTION restore-contract endpoint дозволяв RECEPTIONIST хоча delete-contract вимагає OWNER/ADMIN — асиметрія (RECEPTIONIST міг би «undo» видалення яке сам не мав права робити). Fix: @Roles('OWNER','ADMIN') на POST /:id/contracts/:cid/restore (дзеркалить delete). api+web tsc 0. Vitest 1015/1015. Попередній: 84488164 feat(counterparties): редагування авто у формі контрагента (Pencil у рядок «Авто», editingVehicleId керує PATCH-режимом, saveVehicle об'єднує POST/PATCH — PATCH без customerGarageId бо гараж уже існує, Vehicle тип += vin для prefill, tenant-guard cpIdAtStart+currentCpIdRef, replace-or-push setModalVehicles). Review 0 findings, web tsc 0. Live CREATE→PATCH→DELETE(204) ✓. Попередній: a912938a docs: редагування+soft-delete договору. Попередній: 331faa26 fix(review): точний коментар для optimistic promote-primary у deleteContract (пояснено чому findIndex(same-type)==first у backend-order [isPrimary desc, createdAt asc]: після видалення primary у решти same-type isPrimary=false → тайбрейк за createdAt asc → збігається з беківським промоутом). Попередній: 407dac38 feat(counterparties): редагування + soft-delete договору у формі контрагента (Pencil+Trash у рядок, editingContractId керує PATCH-режимом, saveContract об'єднує POST/PATCH, deleteContract soft+optimistic promote next-primary same-type, всі handler'и з tenant-guard cpIdAtStart). Review 1 SUGGESTION (misleading comment) fixed, web tsc 0. Попередній: 86ba0504 fix(counterparties): вид договору — завжди редагований select, фільтр за типом. Попередній: 22fbd914 docs: FIFO-графік + колонки оплати. Попередній: 441c04aa fix(tester): Bugs #598-#600 (nullable-sort NULLS LAST + CLIENT-у-графіку type-filter + stale docstring). Попередній: 05ebbeb1 fix: PurchaseOrderQueryDto.sortBy whitelist += paymentDate. Попередній: 282d5fba feat(supplier-payments): FIFO-графік по документах + колонки оплати у списку купівлі. Попередній: 2aea04e4 fix(supplier-payments): графік оплат = SettlementAccount.balance, не Σ PO (baseline bug 30953 vs 47 — два джерела правди). Попередній: 6c03e88f docs(skills): add sibling-drift audit protocol to sto-optimize (cycle 3/3). Попередній: 50f50cc6 perf(optimize): hoist 8 sibling hot-path allocations across services + components (cycle 3/3). Попередній: 9c8e3a57 fix(tester): Bug #597 regression-guards for getSchedule cross-field + totals.byDate. Попередній: e02c1288 simplify(review): consolidate UUID regex to @sto/shared + unify displayCounterparty (cycle 3/3). Попередній: 4fcc1ab4 simplify(review): consolidate UUID_RE to lib/utils (cycle 2). Попередній: e7c18561 docs(skills): +2 patterns у sto-optimize. Попередній: 5858213e perf(optimize): hoist hot-path allocations + single-pass schedule totals (cycle 2/3). Попередній: a5fc685e perf(optimize): schedule hot-path indexes + bucket-by-id line lookup у PO.receive (migration 20260830120000 + purchase-orders.service). Попередній: d5d58af7 fix(tester): Bugs #592-#595 (DST-aware spec + QCProvider wrap + SP create hook migration + strict date DTO). Baseline було червоне (API 991/992 + Web 484/488), після коміту зелене (992/992 + 488/488). Попередній: 2026-06-20 — fix(tester): фінальна верифікація 300 E2E тестів. Виправлено 4 failed:
 (1) work-orders.spec.ts:114 + crud-work-order.spec.ts:147/169 — список нарядів не навігує (row click → side-panel, "Відкрити наряд" → edit-modal). Тести очікували URL change. Fix: ID наряду через API + page.goto('/work-orders/<id>').
 (2) vehicles.spec.ts:83 — hardcoded "AA1234BB|2020|Toyota" крихко при E2E-fixture (model "E2E-Model-560110"). Fix: дізнатись make/year/licensePlate авто через API, регенерувати regex.
 (3) invoices.spec.ts:845 — `table tbody tr` матчив skeleton row "Завантаження" → 0 checkbox. Fix: expect.poll на кількість checkbox-ів замість tr.

Bug #572 (HIGH) FIXED: search 500 на pg_trgm `%` оператор — Postgres 42804 "argument of OR must be type boolean, not type text". Prisma надсилав ${q} без типу, planner не міг вирішити operator. Fix: ${qText}::text cast у 3 SQL запитах (counterparties, work-orders, goods).

Bug #573 (CRITICAL) FIXED: API не стартував — @fastify/middie 9.x вимагає fastify 5.x, ми на 4.28. Pin override у pnpm-workspace.yaml до ^8.0.0 (остання fastify-4-сумісна лінія).
```

### Security audit (2026-06-20)

- **CRITICAL fix:** jwt.strategy.ts тепер вимагає JWT_ACCESS_SECRET через `getOrThrow`; fail-fast на старті, fallback на публічно відомий 'dev_access_secret' видалено
- **Перевірено OK:** SQL injection (всі $queryRaw — tagged template Prisma.sql), Auth (bcrypt 12 + refresh httpOnly+sameSite:strict+path:/api/auth), Sensitive data (purchasePrice масковано MECHANIC/RECEPTIONIST у goods.service.ts, SMS phone — `maskPhone()` у sms.processor.ts, EstimatePublicDto guard у work-orders.share-public.spec.ts), Access Control (RolesGuard+JwtAuthGuard глобально, public endpoints тільки booking/setup/work-orders-public), Misconfig (helmet first plugin, ValidationPipe whitelist+forbidNonWhitelisted глобально, CORS WEB_ORIGIN fail-closed, Swagger тільки у dev), XSS (єдиний dangerouslySetInnerHTML у layout.tsx — static literal color-mode boot), CSRF (sameSite:strict на refresh cookie, Bearer-only для mutations), Mass Assignment (whitelist:true strip-ає orgId/role з input), Multi-tenant (orgId з JWT через @OrgContext всюди, ніде з body)
- **Trade-off:** SSE `/dashboard/stream?token=…` — EventSource без custom header API; mitigated throttle 5/min + getOrThrow secret + claims verify
- **HIGH deps:** 28 у залежностях — vitest <3.2.6 (DEV-only RCE), @fastify/middie <=9.3.1 (middleware bypass, у NestJS guards не використовується), undici (jsdom test + Expo mobile), glob (@nestjs/cli dev tool), shell-quote (Expo mobile). Жодна не у production runtime критичного шляху. Окрема ітерація `pnpm update` після sprint.

### Аудит-висновки (2026-06-17 simplify session)

- `COST_PRICE_VISIBLE_ROLES` Set + `canSeeCostPrice()` — дублювання НЕМАЄ. `GOODS_PRICE_VISIBLE_ROLES` не існує; список ролей повторюється у `@Roles('OWNER','ADMIN','STOREKEEPER','ACCOUNTANT')` на `getPriceHistory` (endpoint-level RBAC), але це різні рівні (endpoint vs DTO field mask) — extraction коштує більше ніж економить
- `toPartDto(part, userRole?)` — параметр threaded в усі 3 callsites: `findOne` (line 243), `addPart` (1180), `updatePart` (1263). `duplicate` не повертає parts DTO; `generatePdf` + `findByShareToken` (public share) — навмисно без costPrice
- `recalcTotals()` — спрощено: `Number(actualHours ?? normoHours ?? 0)` замість `actualHours != null ? Number(...) : Number(... ?? 0)`. Semantically identical (Float vs Decimal — для null/0 result same)
- `hasActual` у `work-orders/page.tsx:917` — `Math.abs(totalActualLabor - totalLabor) >= 0.01` мінімально, без зайвих обчислень. Не чіпати
- `CreateWorkOrderModal` colSpans — узгоджені: parts table 9/10 (NONE/VAT), lines table 8/9, tfoot "Разом товарів" 7/6, "Разом робіт" 5, "Факт. роботи" 6/7 — всі парні з колонками colgroup ✓

---

## Останній commit

```
0129d9c2  fix(review): calcVatTotals — VAT на агрегованій базі (дзеркало WO recalcTotals), не per-line.
          — Ревʼю audit-round4 (b2681f28 mobile + ca778c0b web-модалки): 1 РЕАЛЬНА проблема, виправлено.
          — ca778c0b зробив calcVatTotals квантувати ПДВ per-line (посилаючись на invoices/PO calcLineVat),
            але ЄДИНИЙ споживач — CreateWorkOrderModal, а backend work-orders recalcTotals рахує ПДВ ОДИН раз
            на агрегованій базі. 3×2.525@20%: preview vat=1.53 vs saved vat=1.52 → preview≠saved (той самий баг).
          — Fix: total=Σ(roundMoney(qty×price)); vat=roundMoney(base×rate/100) на агрегованій базі. Тест оновлено.
          — Перевірено ЧИСТО: mobile getAccessToken() = той самий in-memory accessToken (getToken видалено, 0 споживачів);
            SR double-submit savingRef/transitioningRef guard ПЕРЕД await + reset у finally + handleModalClose; crypto.randomUUID SSR-safe.
          — web tsc 0, web тести 511/511 (11 WO regression включно).

3a4094e8  review(audit-round3): аудит коміту 3a4094e8 (8 логічних багів) — 0 нових проблем, усі 6 фіксів верифіковано КОРЕКТНИМИ.
          — setup TOCTOU: pg_advisory_xact_lock ПЕРЕД in-tx re-check count(); $executeRaw tagged-tmpl (не Unsafe);
            re-check під READ COMMITTED бачить закомічену org 1-го; happy-path не зламано. Spec РЕАЛЬНИЙ (create not.called при count=1).
          — CAL-C2 findSlots CONTAINMENT→half-open OVERLAP (startAt<end && endAt>start) — ідентично checkConflicts()+createSlot();
            MECHANIC-гілка теж змінена (спільний where); сусідні слоти НЕ включаються хибно (endAt>start виключає endAt==start).
          — CAL-H2 booking: reject requestedAt<Date.now() ПЕРЕД create+SMS; абсолютні інстанти (Date несе offset), не блокує майбутнє.
          — cascade-guards симетричні MD-H1: warehouses (StockItem qty/reserved≠0), zones (active Lift), employees
            (WorkOrderLine.employeeId + WO status notIn ARCHIVED/CANCELLED) — усі orgId-scoped, guard ПЕРЕД soft-delete.
          — ConfirmBookingDto.slotId optional + controller @Body()? прокидання + orgId-валідація (чужа org→NotFound); стара поведінка ціла.
          — brands.restore MD-C1 parity: активний дубль назви→409 (@@unique[orgId,name] без partial WHERE).
          — tsc api 0; 7 affected spec ✅ 66/66 (setup 4, calendar 13, booking 20, brands 9, warehouses 14, zones 3, employees-remove-guard 3).
          Нових патернів для скілів не виявлено. Комміт не потрібен (лише docs).

39e22085  docs(skills): add Prisma upsert × Postgres CHECK approach to sto-tester (Bug #621)
          — §1.1 (Інвентар) +1 CRITICAL checklist + Накопичені підходи +1 approach.
          — Мета-правило: міграція що додає CHECK на існуючу таблицю = обов'язковий live-probe
            кожного upsert/insert-writer з граничними значеннями (unit-мок Prisma не б'є Postgres).

19b7dce9  fix(tester): Bug #621 CRITICAL — WRITEOFF 500 (Prisma upsert × Postgres CHECK) + guards #622/#623
          — Bug #621 CRITICAL: stockItem.upsert create-гілка вставляла від'ємний quantity для
            WRITEOFF. Prisma upsert=INSERT VALUES ON CONFLICT DO UPDATE; Postgres перевіряє CHECK
            stock_items_quantity_nonneg (міграція 20260902210000) на INSERT-tuple ДО арбітражу
            конфлікту → 23514 (500) на КОЖНОМУ WRITEOFF/TRANSFER-out/WO-COMPLETED навіть коли
            рядок існує зі 100 і DO UPDATE дав би 60. З мерджу міграції на main усі списання падали.
          — Fix: create.quantity = Math.max(0, quantityDelta) (дзеркалить reserved-кламп).
          — Live-verified: RECEIPT→WRITEOFF 201 (100→60), OPENING_BALANCE→WRITEOFF 201 (100→60).
          — Bug #622 MEDIUM: pricing scope (Bug #178 AND→precedence) без regression-test → +1 unit.
          — Bug #623 MEDIUM: QTY_EPSILON (d74b7b3d) без дробового epsilon-тесту → +2 unit.
          — Усі 7 фіксів коміту 0729e639 підтверджено коректними. API 1155→1159, tsc 0.

eee7cb8e  fix(sync): fmtAggValue — date agg aliases (MIN/MAX) format as date not money
          — MIN_documentDate/MAX_completedAt раніше рендерились через fmtMoney (Unix-мс → абсурдні гривні).
          — Fix: fmtAggValue(alias, value, cols?) → детектує date-тип через alias→fieldKey→cols lookup.
          — Обидва call-sites оновлені (grandTotals tfoot + GroupRows per-node).
          — Решта 7 пунктів sync-аудиту: розбіжностей 0.

a29ae594  fix(tester): Report Builder Bug #617/#618 — include+select mix + mute Prisma validation
          — Bug #617 CRITICAL: mergeIncludePath для parent-leaf + parent.child.leaf (напр. good.name + good.brand.name)
            генерував include+select на одному рівні → PrismaClientValidationError → 400 mute. Fix: relation-branch
            повністю через select (nested select для subrel — Prisma-canonical); корінь лишається include.
          — Bug #618 LOW-DX: PrismaClientValidationError мовчки → 400 без message-логу (15 хв діагностики #617 замість 1 хв).
            Fix: logger.warn з останнім непорожнім рядком exception.message (Prisma кладе фактичну причину саме туди).
          — Живий інваріант Σ(листкові aggs)==grandTotal підтверджений для 8 сценаріїв (diff=0.000000).
          — Regression guards: +2 unit (builder Bug #617 спільні префікси + multi-hop) + 1 unit (filter Bug #618 warn).
          — Metrics: API 1130→1132 tests (+2), tsc clean.

ad65c36f  fix(review): 5 findings on supplier-payments schedule + PO paymentDate (feat/supplier-payments)
          — getSchedule: counterpartyContract.findMany take=5000, from<=to валідація + cap 100 днів
          — receive: перевірка po.contract.deletedAt перед застосуванням paymentDeferDays
          — PO Modal: disabled={!canEdit} на «Дата оплати» (backend PATCH блокує non-DRAFT)
          — purchase-orders page: invalidate supplierPaymentsKeys/counterpartiesKeys після receive
          — PageClient supplier-payments: React.ReactNode → import type { ReactNode }

65064c6a  docs(skills): SupplierPayment Bug #588 → sto-tester paired-FK approach (feat/supplier-payments)
92390dbc  fix(tester): Bug #588 — supplier-payments update() auto-clears orphan PO on supplier change (feat/supplier-payments)
          — API-only clients могли лишити purchaseOrderId від старого постачальника → cross-supplier linkage
          — Fix: shouldClearOrphanPO = supplierChanged && dto.purchaseOrderId===undefined && sp.purchaseOrderId!==null
48ad57a6  feat(supplier-payments): document + endpoint for paying suppliers (feat/supplier-payments)
          — Нова модель SupplierPayment: оплата постачальнику, FSM DRAFT→CONFIRMED пише SettlementTransaction(PAYMENT)
          — Джерело коштів обов'язкове (bank/cash), опц. прив'язка до PurchaseOrder. БЕЗ Checkbox/loyalty (supplier-side)
          — API /supplier-payments (OWNER/ADMIN/ACCOUNTANT) + web список/модалка/nav. Дос'є: docs/objects/supplier-payment.md
          — Міграції 20260703100000 (модель+enums+DocumentType) + 20260703100001 (backfill DocumentNumberConfig ОПП)
951506b7  fix(review): clear paired purchase-order ref when supplier cleared in SupplierPaymentCreateModal (feat/supplier-payments)
          — §8.2 paired FK state: EntityPickerField.onClear supplier тепер скидає обидві пари (supplierId+supplierName, purchaseOrderId+purchaseOrderNumber)
          — Без фіксу залишався orphan PO → backend 400 без пояснення у UI
d699d0ae  fix(sync): align SupplierPaymentCreateModal with bank-accounts/cash-registers API contracts (feat/supplier-payments)
          — /bank-accounts + /cash-registers return { items, total } not bare array; fix apiFetch typing + destructuring
          — Remove client-side deletedAt filter (server returns only active); remove deletedAt from local interfaces
a59a023f  fix(review): strip BOM from 7 inventory/goods files + $transaction timeouts + React namespace types
          — BOM (§1): 7 files after comment-cleanup Windows/PowerShell edit
          — $transaction timeout (§5): 4 blocks — goods.service (barcode primary swap/delete), settings.service (taxRate default create/update) → { timeout: 10_000 }
          — React.X namespace (§1): CalendarSlotModal.tsx → named type imports (Dispatch/SetStateAction/RefObject)
b28f3411  refactor(comments): remove Bug# references and noise comments; document comment policy in sto-dev
          — Strip `// Bug #NNN:` prefixes from ~180 comments across 130+ production files
          — Preserve WHY text (constraints, workarounds, timeouts, race-guards)
          — sto-dev SKILL.md: add ЗАБОРОНЕНО/ДОЗВОЛЕНО examples
6336f201  test(e2e): hard expects замість silent skip + 2 нових spec (bookings, counterparty-detail)
9b9e2ce0  fix(tester): Bug #572 search 500 + Bug #573 fastify/middie API startup crash
26f3895d  docs(memory): update after E2E expansion — 277/284 pass (+39 tests)
c8ccfdad  test(e2e): Add command-palette + supplier-returns coverage, surface Bug #572 (search 500)
bf2f78a6  test(e2e): Add 30 tests across 5 new specs — profile, ndi, sync, vehicles, calendar-views
7575081d  fix(e2e): Eliminate test.skip(true) fake-green in 5 crud specs — Bug #571 follow-up
4c883670  simplify(review): role-gate spec merge + pnpm-workspace placeholder fix
4c62d12d  fix(review): фінальний огляд 3/3 — pnpm overrides + AuthenticatedUser типізація + regression specs
27210eb2  simplify(e2e): Цикл 3/3 step 6 — extract nextWorkingDayIso + openPoEditModal helpers, drop dead enableDetailPanel
3083ae58  fix(tester): Цикл 3/3 step 5 — Bug #571 (e2e fake-green silent skips)
a0149911  docs(skills): add public hot-path multi-field WHERE compound B-tree drift to sto-optimize
bb48063d  perf(optimize): Цикл 3/3 step 4 — covering index booking_requests(orgId,branchId,status,requestedDate)
667f8798  fix(tester): Цикл 3/3 step 3 — Bug #568-#570 (E2E search fill, Throttle contract, WO labels contract)
2d77c7eb  docs(memory): update MemoryManual after review Цикл 3/3 step 2
3f1527f0  fix(review): Цикл 3/3 step 2 — public booking @Throttle + strip BOM from 10 files
4f1f345d  fix(sync): align WO status labels and StockTotal interface with API contracts (Цикл 3/3 step 1)
487fb807  perf(optimize): parallelize pricing-rules tier+main update; memoize PricingRules filter; dynamic-load SupplierReturnCreateModal (Cycle 2/3 step 4)
e4c72a49  fix(tester): bugs #565/#566/#567 — supplierId contract tests, IPv4 E2E, auth escape-hatch (Cycle 2/3 step 3)
488704b2  fix(review): pricing-rules supplierId UUID guard + strip stray BOM (Cycle 2/3 step 2)
4fe1a559  fix(sync): align frontend hook interfaces with backend response DTOs (Cycle 2/3 step 1)
cce4130a  perf(optimize): parallelize supplier + warehouse validation in SupplierReturn.update()
cba69150  fix(tester): Bug #537 validation message Cyrillic + #538 E2E flaky (Цикл 1/3 step 3)
00f6c657  fix(review): remove UTF-8 BOM from 19 DTO files
968e49b0  fix(api): resolve 37 TypeScript errors — Date→string conversions + undefined variable refs
```

Повна історія → [CHANGELOG.md](CHANGELOG.md)

---

## Нові файли/утиліти (з останніх сесій)

| Файл                                                                  | Що                                                    |
| --------------------------------------------------------------------- | ----------------------------------------------------- |
| `apps/api/src/common/utils/array.ts`                                  | `deduplicateBy<T>(arr, key)` — Map last-wins dedup    |
| `apps/web/src/lib/utils.ts`                                           | `toIdMap<T extends {id}>`, `calcVatTotals`            |
| `packages/shared/src/constants/statuses.ts`                           | RECEIPT у STOCK_DOC_TYPE_LABELS/BADGE                 |
| `apps/web/src/app/(app)/work-orders/[id]/InvoiceSection.tsx`          | Extracted invoice block компонент з PageClient.tsx    |
| `packages/shared/tsconfig.cjs.json`                                   | CJS build config (module: commonjs → dist/cjs/)       |
| `apps/api/src/modules/work-orders/work-orders.role-gate.spec.ts`      | 22 регресія-guard тестів матриці userRole × costPrice |
| `apps/api/src/modules/work-orders/work-orders-export.service.spec.ts` | 4 регресія-guard тести Bug #508/#528 (planned amount) |

---

## Активні особливості поточного коду

- `StockDocumentType.RECEIPT` — повністю додано: Prisma enum + DTO + service + frontend tabs
- `deduplicateBy(plan, u => u.goodId)` — у PO/xlsx applyPricing ПЕРЕД `Promise.all`
- `BALANCE_SIGN: Record<SettlementTransactionType, 1|-1>` — exhaustive (без Partial<>)
- `Promise.all` для per-line writes у SD transition/PO receive (disjoint rows — safe)
- work-orders.service.ts parts loops — **sequential** (shared StockItem composite key — unsafe to parallelize)
- CalendarSlot.parentSlotId — split-day continuation invariant (не колапсувати через updateMany)
- BullMQ API: `@Processor('queue', { concurrency: N })` + `extends WorkerHost` + `async process(job: Job<T>)` (НЕ legacy `@Process({ name, concurrency })`)
- `BullModule.forRootAsync` — `connection: { host, port, password, db }` (НЕ `redis:`)
- `RepeatOptions` — `pattern: '0 9 * * *', tz: 'Europe/Kyiv'` (НЕ `cron:`)
- SMS-канал через `NotificationsService.send(orgId, eventType, payload)` — НЕ прямий `smsQueue.add()`. payload має `branchId` + `phone` + template placeholders. service резолвить branchSettings provider/apiKey + NotificationTemplate.body.
- `NotificationEventType` enum: WO_CREATED/WO_ESTIMATE_READY/WO_APPROVED/WO_IN_PROGRESS/WO_COMPLETED/WO_READY_FOR_PICKUP/PAYMENT_RECEIVED/INVOICE_SENT/LOW_STOCK_ALERT/FOLLOWUP_REMINDER/**BOOKING_CONFIRMATION** (новий)
- **BookingRequest**: публічна сторінка `/booking` (publicFetch, без auth) + адмін `/bookings` (apiFetch + useBookingRequests). Контролер `@Controller('booking')` — GET/PATCH/DELETE захищені JWT, GET branches/availability/request — публічні
- **CalendarSlot.workOrderStatus**: backend DTO + frontend CalendarSlot type + CalendarDayGrid рендер через WO_STATUS_LABELS (@sto/shared)
- **BookingSlot на календарі**: PENDING booking requests відображаються на CalendarDayGrid, assign до першого вільного lift client-side. Тип BookingSlot — client-only
- **GET /goods/stock-totals**: повертає `{ goodId, totalQuantity, byWarehouse[] }[]`, frontend StockTotal читає тільки `{ goodId, totalQuantity }` — byWarehouse ігнорується (валідно)
- **StatusPill компонент**: `apps/web/src/components/ui/status-pill.tsx` — використовується у work-orders, invoices, stock-documents, purchase-orders filter bars
- **`@sto/shared` CJS build**: `packages/shared/package.json` main=`./dist/cjs/index.js`. Запускати `pnpm --filter @sto/shared build:cjs` якщо shared змінювався і API не стартує
- **NestJS SWC на Windows**: SWC не резолвить `tsconfig paths` — вставляє alias як literal string у dist JS. **Рішення: залишити tsc builder** (`nest start --watch` без `--builder swc`). `baseUrl: "."` залишити для SWC compatibility але builder = tsc
- **`rootDir: "src"` у api tsconfig** — обов'язково! Без нього tsc дзеркалить monorepo дерево → `dist/apps/api/src/main.js` замість `dist/main.js` і `node dist/main` падає з MODULE_NOT_FOUND

---

## Довідники (читати за потреби)

| Файл                                                 | Коли читати                                                                                                                                                                                |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)         | API модулі (28), Prisma моделі (41), утиліти, sync                                                                                                                                         |
| [docs/PATTERNS.md](docs/PATTERNS.md)                 | UI компоненти, hooks, B1-B7, EntityPickerField + **Мета-патерни MP-B1..B10/MP-F1..F5** (наскрізні: SOT, централізація мутацій, tenant-guard, config-over-hardcode, enum-axis, атомарність) |
| [docs/BUSINESS-RULES.md](docs/BUSINESS-RULES.md)     | FSM, інвентар, розрахунки, тенант-ізоляція                                                                                                                                                 |
| [docs/GOTCHAS.md](docs/GOTCHAS.md)                   | Відомі пастки — читати перед новою фічею                                                                                                                                                   |
| [CHANGELOG.md](CHANGELOG.md)                         | Журнал комітів по фічах                                                                                                                                                                    |
| [docs/PHASES.md](docs/PHASES.md)                     | Поточна фаза і задачі                                                                                                                                                                      |
| [docs/objects/](docs/objects/)                       | Дос'є агрегатів: WO, Invoice, PO, StockDoc, Counterparty, Good, Work, Calendar                                                                                                             |
| [docs/specs/\_TEMPLATE.md](docs/specs/_TEMPLATE.md)  | Шаблон специфікації нової фічі                                                                                                                                                             |
| [.claude/memory/MEMORY.md](.claude/memory/MEMORY.md) | User preferences                                                                                                                                                                           |

---

## Правило оновлення (для агентів)

Після кожного коміту — оновити **тільки** цей файл:

1. `Останній commit` → нові хеші (5–6 рядків)
2. `Поточний стан` → TypeScript статус, дата, тести
3. `Нові файли/утиліти` → якщо з'явились нові
4. `Активні особливості` → якщо щось змінилось у логіці

**НЕ** додавати сюди деталі рішень, full bug descriptions, список виправлень.  
Деталі → `CHANGELOG.md` (append, 3–5 рядків max per commit).  
Патерн/правило → відповідний довідник (одне місце правди).
